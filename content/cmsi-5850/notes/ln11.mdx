import {
  LectureNotes,
  LectureResources,
} from "@/components/lecture-sections"
import { DefinitionBox } from "@/components/interactive-example"
import { DisplayMath, InlineMath } from "@/components/math"
import { OperationalSemanticsStepper } from "@/components/operational-semantics-stepper"
import { StackMachineStepper } from "@/components/stack-machine-stepper"
import { RegisterMachineStepper } from "@/components/register-machine-stepper"
import { InferenceRuleReader } from "@/components/inference-rule-reader"
import { ProofTreeReader } from "@/components/proof-tree-reader"
import { ProofTreeConstructor } from "@/components/proof-tree-constructor"
import { ProofTreeProgramSplit } from "@/components/proof-tree-program-split"

## Recap

In LN 10, we crossed the boundary from syntax to semantics. We defined the **meaning function** <InlineMath formula="$M : \text{AST} \to \text{Semantic Domain}$" /> and traced through Petal programs informally, watching environments grow and output appear. We confronted the scoping question — lexical vs. dynamic — and previewed three formal approaches.

Today we formalize the first of those approaches: **operational semantics**. The idea is simple: *the meaning of a program is what it does when you run it.* We define an abstract machine and show how the program executes on that machine, step by step. By the end, we'll see that the inference rules we write are not just mathematical notation — they are a blueprint for an **interpreter**.

---

## Today's Agenda

- **The Operational Intuition** — Meaning as simulation: watch what happens
- **Concrete Operational Semantics** — Stack machines as a brief introduction
- **Small-Step (SOS)** — Plotkin's transition relation: one step at a time
- **Big-Step (Natural)** — Kahn's evaluation relation: straight to the result
- **Small-Step vs. Big-Step** — When each approach shines
- **Extending Petal** — Adding if-else and while loops
- **From Rules to Code** — The interpreter reveal
- **Interpreters in the Wild** — How real languages implement tree-walking evaluation
- **Proof Trees** — Using inference rules to prove program properties

---

## The Operational Intuition

What does `3 + 5 * 2` mean? One answer: watch what happens when you compute it.

1. First, compute `5 * 2` — that gives `10`.
2. Then, compute `3 + 10` — that gives `13`.
3. Done. The meaning is `13`.

This is the operational approach: meaning as **simulation**. We define a machine (even if it's abstract), load the program into it, and show what happens when it runs. The machine's behavior *is* the meaning.

> The word "operational" literally means "pertaining to operations." An operational semantics defines meaning by describing the operations that a machine performs. You've been doing this since you first traced through code with pencil and paper — operational semantics just makes that process mathematically precise.

There are multiple ways to formalize this intuition, ranging from very concrete (build an actual virtual machine) to very abstract (write inference rules). We'll briefly mention the concrete approach, then spend the lecture on the two formal approaches that matter most.

---

## Concrete Operational Semantics

One way to define meaning operationally is to **translate** the AST into instructions for a concrete machine. The most famous approach flattens the tree into a linear sequence of instructions for a **stack machine**.

<DefinitionBox term="Stack Machine">

A **stack machine** is an abstract computational device that uses a last-in-first-out (LIFO) stack as its primary data structure. Instructions operate by pushing values onto the stack or popping values to compute results. Many real virtual machines use this model, including the Java Virtual Machine (JVM) and Python's bytecode interpreter (CPython).

</DefinitionBox>

### Engineering COS from the Tree Grammar

Recall from LN 10 that Petal's tree grammar is the **API contract** that any semantic approach must implement. For concrete operational semantics, each production in the grammar tells us what machine instructions to emit:

| Tree Grammar Production | Stack Machine Instruction(s) |
|---|---|
| <InlineMath formula="n" /> (number literal) | `PUSH n` |
| <InlineMath formula="e_1 \oplus e_2" /> (binary op) | <InlineMath formula="T\llbracket e_1 \rrbracket \cdot T\llbracket e_2 \rrbracket \cdot \text{OP}" /> |
| <InlineMath formula="i" /> (identifier) | `LOAD i` |
| <InlineMath formula="i\text{=}e" /> (assignment) | <InlineMath formula="T\llbracket e \rrbracket \cdot \text{STORE } i" /> |
| <InlineMath formula="\textbf{print}\; e" /> (print) | <InlineMath formula="T\llbracket e \rrbracket \cdot \text{PRINT}" /> |

The translation function <InlineMath formula="T" /> is compositional: to translate a compound node, translate its children first, then append the node's own instruction. This is a **post-order traversal** of the AST — the same structural recursion we keep seeing.

<DisplayMath formula="T\llbracket n \rrbracket = \text{PUSH } n \qquad T\llbracket e_1 + e_2 \rrbracket = T\llbracket e_1 \rrbracket \cdot T\llbracket e_2 \rrbracket \cdot \text{ADD}" />

### Watching the Stack Machine

For the program `x = 3 + 5; print x;`, the translation produces:

```
PUSH 3
PUSH 5
ADD
STORE x
LOAD x
PRINT
```

Use the **Translate** tab below to watch how the translation function walks the AST in post-order, emitting one instruction per node. Then switch to **Execute** to watch those instructions run on the stack:

<StackMachineStepper
  title="Stack Machine: x = 3 + 5; print x;"
  astNodes={[
    { id: "prog", label: "P", level: 0 },
    { id: "a1", label: "x = ...", level: 1 },
    { id: "add", label: "+", level: 2 },
    { id: "n3", label: "3", isTerminal: true, level: 3 },
    { id: "n5", label: "5", isTerminal: true, level: 3 },
    { id: "pr", label: "print", level: 1 },
    { id: "idx", label: "x", isTerminal: true, level: 2 },
  ]}
  astEdges={[
    { from: "prog", to: "a1" },
    { from: "prog", to: "pr" },
    { from: "a1", to: "add" },
    { from: "add", to: "n3" },
    { from: "add", to: "n5" },
    { from: "pr", to: "idx" },
  ]}
  instructions={["PUSH 3", "PUSH 5", "ADD", "STORE x", "LOAD x", "PRINT"]}
  translationSteps={[
    { highlightNodes: ["prog"], generatedSoFar: [], description: "Begin translation. We traverse the AST in post-order: visit children before parents." },
    { highlightNodes: ["a1"], generatedSoFar: [], description: "First statement: x = ... — translate the RHS expression before the assignment." },
    { highlightNodes: ["add"], generatedSoFar: [], description: "RHS is (+). Post-order: translate left child, then right child, then emit ADD." },
    { highlightNodes: ["n3"], generatedSoFar: ["PUSH 3"], description: "Left child is the literal 3. Emit PUSH 3." },
    { highlightNodes: ["n5"], generatedSoFar: ["PUSH 3", "PUSH 5"], description: "Right child is the literal 5. Emit PUSH 5." },
    { highlightNodes: ["add"], generatedSoFar: ["PUSH 3", "PUSH 5", "ADD"], description: "Both children translated. Emit ADD for the (+) node." },
    { highlightNodes: ["a1"], generatedSoFar: ["PUSH 3", "PUSH 5", "ADD", "STORE x"], description: "Expression translated. Emit STORE x for the assignment." },
    { highlightNodes: ["pr"], generatedSoFar: ["PUSH 3", "PUSH 5", "ADD", "STORE x"], description: "Second statement: print x — translate the expression first." },
    { highlightNodes: ["idx"], generatedSoFar: ["PUSH 3", "PUSH 5", "ADD", "STORE x", "LOAD x"], description: "The expression is the identifier x. Emit LOAD x." },
    { highlightNodes: ["pr"], generatedSoFar: ["PUSH 3", "PUSH 5", "ADD", "STORE x", "LOAD x", "PRINT"], description: "Emit PRINT. Translation complete — 6 instructions from 7 AST nodes." },
  ]}
  executionSteps={[
    { pc: 0, stack: [], highlightNodes: ["n3"], description: "Machine starts. The stack is empty, PC points to instruction 0." },
    { pc: 1, stack: [3], highlightNodes: ["n5"], description: "PUSH 3 — push the literal 3 onto the stack." },
    { pc: 2, stack: [3, 5], highlightNodes: ["add"], description: "PUSH 5 — push 5. The stack now has two values." },
    { pc: 3, stack: [8], highlightNodes: ["a1"], description: "ADD — pop 5 and 3, push their sum 8. The stack shrinks." },
    { pc: 4, stack: [], highlightNodes: ["pr", "idx"], description: "STORE x — pop 8 and bind it to x in memory. Stack is empty again." },
    { pc: 5, stack: [8], highlightNodes: ["pr"], description: "LOAD x — look up x in memory (8) and push it onto the stack." },
    { pc: 6, stack: [], highlightNodes: ["prog"], description: "PRINT — pop 8 and emit it to the output stream. Execution complete." },
  ]}
/>

The key insight: the **tree structure of the AST has been flattened** into a linear sequence. The translation tab makes this visible — the post-order traversal visits leaves before parents, ensuring operands are pushed before the operator that consumes them. The stack reconstructs the nesting at runtime.

> The JVM is one of the most successful stack machines ever built. Every Java, Kotlin, Scala, and Clojure program compiles down to JVM bytecode — a sequence of stack instructions not unlike the `PUSH`/`ADD` above, just with hundreds more operations. The operational semantics of the JVM is defined by the [JVM Specification](https://docs.oracle.com/javase/specs/jvms/se21/html/), a 600+ page document that describes every instruction's effect on the stack, heap, and execution state.

### The Register Machine Alternative

Stack machines aren't the only way to build a concrete machine. **Register machines** use a fixed set of named storage locations (registers) instead of an anonymous stack. The translation function changes fundamentally: instead of a simple post-order traversal with implicit operands, the translator must now **allocate a fresh register** for each intermediate result and **reference child registers by name** in parent instructions.

<DefinitionBox term="Register Machine">

A **register machine** is an abstract computational device that uses a fixed set of named registers (fast temporary storage locations) as its primary workspace. Instructions specify operands explicitly by register name, rather than relying on stack position. Most physical CPUs (x86, ARM, RISC-V) and modern compiler intermediate representations (LLVM IR) use this model.

</DefinitionBox>

Watch the **Translate** tab to see how the same AST produces a different instruction sequence — notice how the translator allocates registers `r1`, `r2`, `r3`, ... as it visits each node. Then switch to **Execute** to watch the register file fill up:

<RegisterMachineStepper
  title="Register Machine: x = 3 + 5; print x;"
  astNodes={[
    { id: "prog", label: "P", level: 0 },
    { id: "a1", label: "x = ...", level: 1 },
    { id: "add", label: "+", level: 2 },
    { id: "n3", label: "3", isTerminal: true, level: 3 },
    { id: "n5", label: "5", isTerminal: true, level: 3 },
    { id: "pr", label: "print", level: 1 },
    { id: "idx", label: "x", isTerminal: true, level: 2 },
  ]}
  astEdges={[
    { from: "prog", to: "a1" },
    { from: "prog", to: "pr" },
    { from: "a1", to: "add" },
    { from: "add", to: "n3" },
    { from: "add", to: "n5" },
    { from: "pr", to: "idx" },
  ]}
  instructions={["LOAD r1, 3", "LOAD r2, 5", "ADD r3, r1, r2", "STORE x, r3", "LOAD r4, x", "PRINT r4"]}
  translationSteps={[
    { highlightNodes: ["prog"], generatedSoFar: [], description: "Begin translation. Like the stack machine, we traverse the AST — but now we must allocate a register for each result." },
    { highlightNodes: ["a1"], generatedSoFar: [], description: "First statement: x = ... — translate the RHS expression, tracking which register holds its result." },
    { highlightNodes: ["add"], generatedSoFar: [], description: "RHS is (+). We need its children's results in registers before we can emit ADD." },
    { highlightNodes: ["n3"], generatedSoFar: ["LOAD r1, 3"], description: "Left child: literal 3. Allocate r1, emit LOAD r1, 3. The translator remembers: this node's result is in r1." },
    { highlightNodes: ["n5"], generatedSoFar: ["LOAD r1, 3", "LOAD r2, 5"], description: "Right child: literal 5. Allocate r2, emit LOAD r2, 5. This node's result is in r2." },
    { highlightNodes: ["add"], generatedSoFar: ["LOAD r1, 3", "LOAD r2, 5", "ADD r3, r1, r2"], description: "Back at (+): children are in r1 and r2. Allocate r3 for the result, emit ADD r3, r1, r2." },
    { highlightNodes: ["a1"], generatedSoFar: ["LOAD r1, 3", "LOAD r2, 5", "ADD r3, r1, r2", "STORE x, r3"], description: "RHS result is in r3. Emit STORE x, r3 to bind x in memory." },
    { highlightNodes: ["pr"], generatedSoFar: ["LOAD r1, 3", "LOAD r2, 5", "ADD r3, r1, r2", "STORE x, r3"], description: "Second statement: print x — translate the expression." },
    { highlightNodes: ["idx"], generatedSoFar: ["LOAD r1, 3", "LOAD r2, 5", "ADD r3, r1, r2", "STORE x, r3", "LOAD r4, x"], description: "Identifier x: allocate r4, emit LOAD r4, x to load from memory into a register." },
    { highlightNodes: ["pr"], generatedSoFar: ["LOAD r1, 3", "LOAD r2, 5", "ADD r3, r1, r2", "STORE x, r3", "LOAD r4, x", "PRINT r4"], description: "Emit PRINT r4. Translation complete — every operand is explicitly named." },
  ]}
  executionSteps={[
    { pc: 0, registers: {}, highlightNodes: ["n3"], description: "Machine starts. No registers allocated yet." },
    { pc: 1, registers: { r1: 3 }, highlightRegister: "r1", highlightNodes: ["n3"], description: "LOAD r1, 3 — register r1 now holds 3." },
    { pc: 2, registers: { r1: 3, r2: 5 }, highlightRegister: "r2", highlightNodes: ["n5"], description: "LOAD r2, 5 — register r2 now holds 5. Notice: r1 still holds 3 (values persist)." },
    { pc: 3, registers: { r1: 3, r2: 5, r3: 8 }, highlightRegister: "r3", highlightNodes: ["add"], description: "ADD r3, r1, r2 — read r1 (3) and r2 (5), store sum 8 in r3. All three registers retain their values." },
    { pc: 4, registers: { r1: 3, r2: 5, r3: 8 }, memory: { x: 8 }, highlightNodes: ["a1"], description: "STORE x, r3 — copy r3 (8) into memory location x. Registers unchanged." },
    { pc: 5, registers: { r1: 3, r2: 5, r3: 8, r4: 8 }, memory: { x: 8 }, highlightRegister: "r4", highlightNodes: ["idx"], description: "LOAD r4, x — load x (8) from memory into r4." },
    { pc: 6, registers: { r1: 3, r2: 5, r3: 8, r4: 8 }, memory: { x: 8 }, highlightNodes: ["pr"], description: "PRINT r4 — emit 8 to the output stream. Execution complete. All registers still hold their values." },
  ]}
/>

Compare the two translation approaches: the stack machine used a simple post-order traversal with no bookkeeping — operands land on the stack implicitly. The register machine required the translator to **track which register holds each sub-expression's result** and **pass register names upward** to parent nodes. This is fundamentally more complex, but it has an advantage: register values **persist** after use. In the stack machine, values disappear the moment they're popped; in the register machine, `r1` still holds `3` even after the `ADD` consumed it. This makes register machines easier to debug and optimize — but harder to generate code for (register allocation is NP-hard in the general case).

| Feature | Stack Machine | Register Machine |
|---|---|---|
| **Operands** | Implicit (top of stack) | Explicit (named registers) |
| **Instruction size** | Smaller (no operand names) | Larger (register names in every instruction) |
| **Temporary values** | Pushed/popped automatically | Allocated to named registers |
| **Value lifetime** | Consumed on use (pop) | Persist until overwritten |
| **Real-world examples** | JVM, CPython, WebAssembly (MVP) | x86, ARM, LLVM IR, Dalvik (Android) |
| **Code generation** | Simpler (post-order traversal) | More complex (register allocation needed) |

> The choice between stack and register architectures is one of the deepest pragmatic decisions in language implementation. The JVM chose a stack architecture for portability (no assumptions about how many registers the host CPU has). LLVM chose a register architecture for performance (it maps more naturally to real hardware). Both are "correct" — they define the same meaning for the same programs. The difference is engineering, not semantics.

This approach is valuable (it's essentially how real VMs and CPUs work), but for our purposes, two more abstract formulations are more powerful. They let us reason about programs without committing to a particular machine architecture.

---

## Structural Operational Semantics (SOS) — Small-Step

The first formal approach was developed by Gordon Plotkin in 1981. The idea: define a **transition relation** that describes how a program configuration changes in a single step of computation.

<DefinitionBox term="Configuration">

A **configuration** is a snapshot of the machine's state at a single point in time. For Petal, a configuration is a pair $\langle e, \sigma \rangle$ where $e$ is an expression (or statement) and $\sigma$ is the current state (environment).

</DefinitionBox>

<DefinitionBox term="Small-Step (Structural) Operational Semantics">

**Small-step semantics** (also called **Structural Operational Semantics** or **SOS**) defines a **transition relation** $\to$ that describes one step of computation:

<DisplayMath formula="\langle e, \sigma \rangle \to \langle e', \sigma' \rangle" />

This reads: "In state $\sigma$, expression $e$ reduces in one step to $e'$, producing new state $\sigma'$."

A program executes by taking steps until no more transitions apply — the configuration is **terminal** (a final value).

</DefinitionBox>

> Gordon Plotkin's 1981 paper "A Structural Approach to Operational Semantics" is one of the most cited papers in programming language theory. His key innovation was making the rules *structural* — defined by induction on the syntax. This means you can determine which rule applies by looking at the top-level constructor of the expression. It's a direct consequence of compositionality: the step for a compound expression is determined by the steps of its parts.

The transition relation is defined by **inference rules** — exactly the kind of logical rules we studied in LN 2. Each rule has premises above the line and a conclusion below:

<DisplayMath formula="\frac{\text{premise}_1 \quad \text{premise}_2 \quad \cdots}{\text{conclusion}}" />

<DefinitionBox term="Inference Rule (Semantic)">

A **semantic inference rule** specifies one step of evaluation. The premises (above the line) describe conditions that must hold; the conclusion (below the line) describes the step that follows. Rules with no premises are called **axioms** — they apply unconditionally.

</DefinitionBox>

### Reading Inference Rules: The Omega Technique

Inference rules look intimidating at first — a fraction with Greek letters, angle brackets, and arrows. But there's a simple, repeatable technique for reading any rule. Trace an **Omega (Ω)** path through it:

1. **Start at the bottom-left** of the conclusion — this is what you *have* (the current configuration).
2. **Loop up** through each premise above the line — these are the conditions you must check or sub-evaluations you must perform.
3. **Come back down to the bottom-right** of the conclusion — this is what you *get* (the resulting value or new configuration).

The shape of this path — bottom-left, up, back down to bottom-right — traces an **Ω**. It works for every inference rule in this course. Let's practice it on the three small-step rules for binary operations — each covers a different case:

<InferenceRuleReader
  title="Reading the Small-Step Addition Rules"
  rules={[
    {
      ruleName: "Left operand steps",
      ruleDescription: "Case 1: The left operand e₁ is not yet a value. We take one step inside e₁, and the whole expression steps along with it. This rule enforces left-to-right evaluation order.",
      conclusion: {
        left: {
          label: "Current configuration",
          latex: "\\langle e_1 \\oplus e_2, \\sigma \\rangle",
          description: "Start here (bottom-left): you have a binary expression e₁ ⊕ e₂ in state σ. The left operand e₁ is not yet fully evaluated."
        },
        right: {
          label: "After one step",
          latex: "\\to \\langle e_1' \\oplus e_2, \\sigma \\rangle",
          description: "End here (bottom-right): the expression steps to e₁' ⊕ e₂ — only the left operand changed. The right operand e₂ and state σ are untouched."
        }
      },
      premises: [
        {
          label: "Left operand can step",
          latex: "\\langle e_1, \\sigma \\rangle \\to \\langle e_1', \\sigma \\rangle",
          description: "The only premise: e₁ can take a single step to e₁' in the current state. If this holds, the whole expression takes a step. This is a recursive small-step judgment."
        }
      ]
    },
    {
      ruleName: "Right operand steps",
      ruleDescription: "Case 2: The left operand is already a value v₁, but the right operand e₂ still needs work. We step inside e₂. This rule only fires when the left side is done — enforcing the left-to-right order.",
      conclusion: {
        left: {
          label: "Current configuration",
          latex: "\\langle v_1 \\oplus e_2, \\sigma \\rangle",
          description: "Start here (bottom-left): you have v₁ ⊕ e₂ where v₁ is already a value. Only e₂ remains to be reduced."
        },
        right: {
          label: "After one step",
          latex: "\\to \\langle v_1 \\oplus e_2', \\sigma \\rangle",
          description: "End here (bottom-right): the expression steps to v₁ ⊕ e₂' — only the right operand changed. The value v₁ stays fixed."
        }
      },
      premises: [
        {
          label: "Right operand can step",
          latex: "\\langle e_2, \\sigma \\rangle \\to \\langle e_2', \\sigma \\rangle",
          description: "The only premise: e₂ takes a single step to e₂'. The left side v₁ is already done, so we focus entirely on the right."
        }
      ]
    },
    {
      ruleName: "Both values — compute",
      ruleDescription: "Case 3: Both operands are values. No more sub-expressions to reduce. But this is not an axiom — it has a side condition above the line that maps the syntactic operator ⊕ to its mathematical meaning.",
      conclusion: {
        left: {
          label: "Current configuration",
          latex: "\\langle v_1 \\oplus v_2, \\sigma \\rangle",
          description: "Start here (bottom-left): you have v₁ ⊕ v₂ where both sides are values. The expression is ready to compute, but we still need the side condition to know what ⊕ means."
        },
        right: {
          label: "Result",
          latex: "\\to \\langle v, \\sigma \\rangle",
          description: "End here (bottom-right): the expression reduces to v — the value produced by the side condition. The syntactic operator has been interpreted as math, and v is the result."
        }
      },
      premises: [
        {
          label: "Apply the operator",
          latex: "v_1 \\oplus v_2 = v",
          description: "The side condition: interpret the syntactic operator ⊕ as its mathematical counterpart. If ⊕ is +, compute v₁ + v₂. If ⊕ is *, compute v₁ × v₂. This premise bridges syntax and semantics for the operator terminal."
        }
      ]
    }
  ]}
/>

> The Omega technique works for every inference rule you'll encounter — in this lecture, in LN 12, in LN 13, and beyond. Whenever you see a new rule, trace the Ω: what do I have? what must I check? what do I get? Notice how the three small-step rules above split one grammar production into three cases based on evaluation order — the tree grammar tells you *what* to handle, and the Ω path tells you *how* to read the result.

#### Why ⊕? The Metavariable Trick

You may have noticed that the rules above use <InlineMath formula="\oplus" /> rather than a specific operator like `+` or `*`. That's because <InlineMath formula="\oplus" /> is a **metavariable** — it stands for *any* of Petal's six binary operators (`+`, `-`, `*`, `/`, `%`, `**`). The side condition <InlineMath formula="v_1 \oplus v_2 = v" /> is the bridge: it says "interpret this syntactic operator as its corresponding mathematical operation and compute the result."

Without the metavariable, we would need to write three rules for *each* operator — left-operand steps, right-operand steps, and compute. Just the compute rules alone would look like:

<DisplayMath formula="\frac{v_1 + v_2 = v}{\langle v_1 + v_2, \sigma \rangle \to \langle v, \sigma \rangle} \quad \frac{v_1 - v_2 = v}{\langle v_1 - v_2, \sigma \rangle \to \langle v, \sigma \rangle} \quad \frac{v_1 \times v_2 = v}{\langle v_1 \times v_2, \sigma \rangle \to \langle v, \sigma \rangle} \quad \cdots" />

That's 6 operators × 3 rules each = **18 rules** for binary operations alone. The metavariable <InlineMath formula="\oplus" /> collapses all 18 into just 3 parametric rules — one rule *family* that covers every operator. This is standard practice in formal semantics: whenever a set of rules differs only in the operator symbol, factor it out with a metavariable and let a side condition do the dispatch.

### Engineering SOS Rules from the Tree Grammar

Before we present the full rule set, let's derive one rule from scratch. This exercise shows that the rules are not arbitrary — they are **mechanically determined** by the tree grammar. Consider the binary operation production <InlineMath formula="e_1 \oplus e_2" />:

**Step 1: Identify the node.** The tree grammar tells us the AST has a binary operator node with two children.

**Step 2: Identify the children.** The grammar says it has two sub-expressions, <InlineMath formula="e_1" /> and <InlineMath formula="e_2" />.

**Step 3: Formulate the small-step strategy.** SOS reduces *one* sub-expression per step. We must choose an evaluation order. We choose **left-to-right**. This gives us three cases:

1. If <InlineMath formula="e_1" /> is not yet a value, reduce it — the whole expression takes a step by reducing its left child.
2. If <InlineMath formula="e_1" /> is a value but <InlineMath formula="e_2" /> is not, reduce <InlineMath formula="e_2" /> — the whole expression takes a step by reducing its right child.
3. If both are values, apply the operator to get the final value.

**Step 4: Write the rules.** Each case becomes one inference rule:

<DisplayMath formula="\frac{\langle e_1, \sigma \rangle \to \langle e_1', \sigma \rangle}{\langle e_1 \oplus e_2, \sigma \rangle \to \langle e_1' \oplus e_2, \sigma \rangle} \quad \frac{\langle e_2, \sigma \rangle \to \langle e_2', \sigma \rangle}{\langle v_1 \oplus e_2, \sigma \rangle \to \langle v_1 \oplus e_2', \sigma \rangle} \quad \frac{v_1 \oplus v_2 = v}{\langle v_1 \oplus v_2, \sigma \rangle \to \langle v, \sigma \rangle}" />

Three rules from one grammar production. The tree grammar told us *what* to handle; the evaluation-order decision told us *how many* rules we need. A right-to-left order would produce three different rules. A parallel evaluation strategy would produce yet another set. The grammar constrains; the semanticist chooses.

> This is the key insight: you don't *memorize* inference rules. You **derive** them from the syntax. The tree grammar is the API; the inference rules are the implementation. Every production becomes one or more rules, and the structure of the production tells you what the premises must be.

### Rules for Petal Expressions

Here are the small-step rules for Petal's expression language:

**Numerals** — map the syntactic numeral to a mathematical number:

<DisplayMath formula="\frac{\mathcal{N}\llbracket \overline{n} \rrbracket = n}{\langle \overline{n}, \sigma \rangle \to \langle n, \sigma \rangle}" />

This rule deserves attention because it reveals something fundamental. The <InlineMath formula="\overline{n}" /> on the left is a **numeral** — a syntactic object, a sequence of digit characters sitting in the AST. The <InlineMath formula="n" /> on the right is a **number** — a mathematical object living in the semantic domain of values. They are *not* the same thing. The string `"42"` and the integer 42 belong to entirely different worlds.

The valuation function <InlineMath formula="\mathcal{N}\llbracket \cdot \rrbracket" /> is the bridge between them. It interprets a syntactic numeral as its mathematical value — the same kind of syntax-to-semantics mapping that <InlineMath formula="\sigma(x)" /> performs for identifiers and <InlineMath formula="v_1 \oplus v_2 = v" /> performs for operators. In fact, every leaf node in the AST has such a bridge:

| AST Leaf | Bridge | From (Syntax) | To (Semantics) |
|----------|--------|----------------|-----------------|
| Numeral <InlineMath formula="\overline{n}" /> | <InlineMath formula="\mathcal{N}\llbracket \overline{n} \rrbracket = n" /> | Digit string in the AST | Mathematical number |
| Identifier <InlineMath formula="x" /> | <InlineMath formula="\sigma(x) = v" /> | Name token in the AST | Value bound in the environment |
| Operator <InlineMath formula="\oplus" /> | <InlineMath formula="v_1 \oplus v_2 = v" /> | Operator terminal in the AST | Mathematical operation on values |

This is the job of semantics: translate *every* syntactic entity into its corresponding semantic domain. The inference rules make each translation explicit through side conditions and premises. Nothing crosses from syntax to meaning silently.

> Many textbooks elide this distinction and write <InlineMath formula="\langle n, \sigma \rangle \to \langle n, \sigma \rangle" /> — treating numeral and number as identical. That works when the mapping is "obvious" (the numeral `42` maps to 42), but it hides the conceptual step. We keep the valuation function explicit because it reinforces the pattern: *every* rule is a syntax-to-semantics translation, and every leaf in the tree grammar must have its own bridge.

> **A principle that follows from the bridge perspective:** whenever a *specific value* would appear in a syntactic position inside a rule, lift it to a metavariable with a side condition. For example, instead of writing <InlineMath formula="\langle \textbf{if}\; 0\; \textbf{then}\; \ldots \rangle" /> with the numeral `0` baked into the conclusion, write <InlineMath formula="\langle \textbf{if}\; v\; \textbf{then}\; \ldots \rangle" /> with the side condition <InlineMath formula="v = 0" /> above the bar. This keeps every rule honest about what is syntax (the `v` in the conclusion) and what is semantics (the value comparison in the premise). We will apply this principle consistently to the if-else and while rules below.

**Identifiers** look up the environment:

<DisplayMath formula="\frac{\sigma(x) = v}{\langle x, \sigma \rangle \to \langle v, \sigma \rangle}" />

**Binary operations** — evaluate left operand first, then right, then apply:

<DisplayMath formula="\frac{\langle e_1, \sigma \rangle \to \langle e_1', \sigma \rangle}{\langle e_1 \oplus e_2, \sigma \rangle \to \langle e_1' \oplus e_2, \sigma \rangle}" />

<DisplayMath formula="\frac{\langle e_2, \sigma \rangle \to \langle e_2', \sigma \rangle}{\langle v_1 \oplus e_2, \sigma \rangle \to \langle v_1 \oplus e_2', \sigma \rangle}" />

<DisplayMath formula="\frac{v_1 \oplus v_2 = v}{\langle v_1 \oplus v_2, \sigma \rangle \to \langle v, \sigma \rangle}" />

Notice the left-to-right evaluation order: we reduce $e_1$ until it's a value, *then* reduce $e_2$. This is a design choice — we could have defined right-to-left or even parallel evaluation. The rules make the choice explicit.

> This is where formal semantics earns its keep. Remember from LN 10, the expression `(x = 2) + x` had different meanings depending on evaluation order. Small-step rules eliminate this ambiguity: the left operand is fully reduced before the right operand is touched. A different set of rules would give a different semantics — and both would be mathematically precise.

### Connection to LN 4

Here's the key insight: **you already know small-step operational semantics**. Beta reduction in the lambda calculus:

<DisplayMath formula="(\lambda x.\, e) \; v \to e[v/x]" />

This is a small-step transition rule. The lambda calculus reduction rules from LN 4 *are* structural operational semantics. You've been doing SOS since week four — you just didn't have the name for it yet.

The evaluation strategies from LN 4 also map directly:

| LN 4 Term | SOS Term |
|-----------|----------|
| Beta reduction | Transition step |
| Normal form | Terminal configuration |
| Call-by-value | Reduce arguments before applying |
| Call-by-name | Substitute arguments unreduced |
| Church-Rosser | Confluence of the transition relation |

> This connection is deep. The lambda calculus, developed by Church in the 1930s, was the first operational semantics — decades before the term existed. When Plotkin formalized SOS in 1981, he was generalizing what Church had already been doing for pure functions. Operational semantics extends the lambda calculus's reduction approach to handle assignment, state, control flow, and all the other features that separate programming languages from pure mathematics.

### Determinism and Non-Determinism

Our rules for Petal are **deterministic** — for any configuration, at most one rule applies. The left-to-right evaluation order ensures this: we always know which subexpression to reduce next. But SOS can also describe **non-deterministic** systems by providing multiple rules for the same configuration. This is essential for modeling concurrent programs, where multiple threads can take steps in any order.

---

## Natural Semantics — Big-Step

The second formal approach was developed by Gilles Kahn in 1987. Instead of showing each tiny step, big-step semantics defines how an expression evaluates to its **final value** in one judgment.

<DefinitionBox term="Big-Step (Natural) Semantics">

**Big-step semantics** (also called **Natural Semantics**) defines an **evaluation relation** $\Downarrow$ that relates a configuration to its final result:

<DisplayMath formula="\langle e, \sigma \rangle \Downarrow v" />

This reads: "In state $\sigma$, expression $e$ evaluates to value $v$."

The relation is defined by inference rules where the premises are themselves big-step evaluations.

</DefinitionBox>

> Gilles Kahn, who worked at INRIA in France, chose the name "natural semantics" because the inference rules feel natural — they describe what the programmer would say if asked "what does this expression evaluate to?" They also mirror the structure of natural deduction from LN 2.

### Engineering Big-Step Rules from the Tree Grammar

Let's repeat the engineering exercise from the SOS section, but now for big-step. Take the same production, <InlineMath formula="e_1 \oplus e_2" />:

**Step 1: Identify the node and children.** Same as before — a binary operator with two sub-expressions.

**Step 2: Formulate premises.** Big-step evaluates each child to its *final value* in one judgment. We don't need to specify evaluation order within a single rule — both children are evaluated to completion:

<DisplayMath formula="\langle e_1, \sigma \rangle \Downarrow v_1 \qquad \langle e_2, \sigma \rangle \Downarrow v_2 \qquad v_1 \oplus v_2 = v" />

**Step 3: Formulate conclusion.** Combine the results into a single judgment for the whole expression:

<DisplayMath formula="\langle e_1 \oplus e_2, \sigma \rangle \Downarrow v" />

**Step 4: Assemble the rule.**

<DisplayMath formula="\frac{\langle e_1, \sigma \rangle \Downarrow v_1 \quad \langle e_2, \sigma \rangle \Downarrow v_2 \quad v_1 \oplus v_2 = v}{\langle e_1 \oplus e_2, \sigma \rangle \Downarrow v}" />

**One rule** instead of three. Big-step is more compact because it doesn't specify evaluation order *within* the rule — each premise is an independent judgment that evaluates a sub-expression all the way to a value. The trade-off: big-step hides the intermediate steps, which makes it simpler to write but harder to use for debugging or reasoning about non-termination.

Now let's apply the Omega technique to a **statement** rule — the assignment <InlineMath formula="i\text{=}e" />. Notice how the configuration shape changes: statements transform the entire machine state (environment + output), not just expressions.

<InferenceRuleReader
  title="Reading the Assignment Rule"
  ruleName="Assignment (Big-Step)"
  conclusion={{
    left: {
      label: "Current configuration",
      latex: "\\langle x = e,\\; \\sigma,\\; o \\rangle",
      description: "Start here: you have an assignment statement 'x = e' in environment σ with current output o."
    },
    right: {
      label: "New configuration",
      latex: "\\Downarrow \\langle \\sigma[x \\mapsto v],\\; o \\rangle",
      description: "End here: the environment is updated — x now maps to v. The output o is unchanged (assignments don't print)."
    }
  }}
  premises={[
    {
      label: "Evaluate the right-hand side",
      latex: "\\langle e,\\; \\sigma \\rangle \\Downarrow v",
      description: "The only premise: evaluate the expression e in the current environment σ. It produces a value v. This is a recursive big-step judgment — it may itself require further evaluation."
    }
  ]}
/>

Compare this to the binary operation rule: the assignment has only **one premise** (evaluate the RHS) because it has only one sub-expression child. The tree grammar's structure directly determines the number of premises. One child, one premise. Two children, two premises (or more, if there are side conditions).

### Rules for Petal Expressions

**Numerals** — the valuation function maps syntax to value (same bridge as in SOS):

<DisplayMath formula="\frac{\mathcal{N}\llbracket \overline{n} \rrbracket = n}{\langle \overline{n}, \sigma \rangle \Downarrow n}" />

**Identifiers** look up the environment:

<DisplayMath formula="\frac{\sigma(x) = v}{\langle x, \sigma \rangle \Downarrow v}" />

**Binary operations** — evaluate both operands, then apply:

<DisplayMath formula="\frac{\langle e_1, \sigma \rangle \Downarrow v_1 \quad \langle e_2, \sigma \rangle \Downarrow v_2 \quad v_1 \oplus v_2 = v}{\langle e_1 \oplus e_2, \sigma \rangle \Downarrow v}" />

### Rules for Petal Statements

**Assignment** — evaluate the expression, update the environment:

<DisplayMath formula="\frac{\langle e, \sigma \rangle \Downarrow v}{\langle x = e, \sigma, o \rangle \Downarrow \langle \sigma[x \mapsto v], o \rangle}" />

**Print** — evaluate the expression, append to output:

<DisplayMath formula="\frac{\langle e, \sigma \rangle \Downarrow v}{\langle \text{print } e, \sigma, o \rangle \Downarrow \langle \sigma, o \cdot v \rangle}" />

**Sequential execution** — thread state through:

<DisplayMath formula="\frac{\langle s_1, \sigma, o \rangle \Downarrow \langle \sigma', o' \rangle \quad \langle s_2, \sigma', o' \rangle \Downarrow \langle \sigma'', o'' \rangle}{\langle s_1; s_2, \sigma, o \rangle \Downarrow \langle \sigma'', o'' \rangle}" />

> The sequencing rule is where **state threading** happens. Notice how $\sigma'$ appears as *output* of the first premise and *input* of the second. The state flows from one statement to the next like water through a pipeline. This is the formal counterpart of the imperative programmer's intuition that statements execute "in order" — the environment produced by one statement becomes the starting environment for the next.

### What Do Statements Look Like Small-Step by Step?

Having just seen the big-step rules for statements, you might wonder: what do these same constructs look like under the small-step lens? In the SOS section above, we only covered expressions. Let's complete the picture by deriving the small-step rules for statements.

The key difference: a statement configuration includes the output stream — $\langle s, \sigma, o \rangle$ — and a completed statement produces a terminal configuration $\langle \sigma', o' \rangle$ (no remaining statement to execute). Each rule follows the same pattern we saw with expressions: if a sub-expression can step, step it; if everything is a value, perform the side effect.

**Assignment** — reduce the RHS expression, then bind:

<DisplayMath formula="\frac{\langle e, \sigma \rangle \to \langle e', \sigma \rangle}{\langle x\text{=}e, \sigma, o \rangle \to \langle x\text{=}e', \sigma, o \rangle}" />

<DisplayMath formula="\frac{}{\langle x\text{=}v, \sigma, o \rangle \to \langle \sigma[x \mapsto v], o \rangle}" />

The first rule steps the right-hand side expression. The second is an axiom: once the RHS is a value, the assignment completes by updating the environment. Compare this to the big-step version — big-step collapses both into a single judgment, while small-step separates "evaluate the expression" from "perform the binding."

**Print** — reduce the argument expression, then emit:

<DisplayMath formula="\frac{\langle e, \sigma \rangle \to \langle e', \sigma \rangle}{\langle \text{print } e, \sigma, o \rangle \to \langle \text{print } e', \sigma, o \rangle}" />

<DisplayMath formula="\frac{}{\langle \text{print } v, \sigma, o \rangle \to \langle \sigma, o \cdot v \rangle}" />

Same pattern: step the argument until it's a value, then append to the output stream.

**Sequential execution** — step the first statement, then continue:

<DisplayMath formula="\frac{\langle s_1, \sigma, o \rangle \to \langle s_1', \sigma', o' \rangle}{\langle s_1;\, s_2, \sigma, o \rangle \to \langle s_1';\, s_2, \sigma', o' \rangle}" />

<DisplayMath formula="\frac{\langle s_1, \sigma, o \rangle \to \langle \sigma', o' \rangle}{\langle s_1;\, s_2, \sigma, o \rangle \to \langle s_2, \sigma', o' \rangle}" />

The first rule says: if the first statement can take a step (but isn't done yet), the whole sequence takes a step. The second rule says: when the first statement completes (reaches a terminal configuration), discard it and move to the second statement with the updated state. This is where you can see the state threading happen *one step at a time*, compared to big-step's all-at-once approach.

> Notice the structural parallel: each big-step statement rule has a corresponding *pair* of SOS rules — one for "the sub-expression is still reducing" and one for "the sub-expression is done, perform the effect." Big-step merges these into a single rule with a recursive premise. This is the fundamental trade-off: big-step is more compact, small-step shows more detail.

### Propagation vs. Completion: A Pattern for All SOS Rules

If you look back at every small-step rule we've written, each one falls into exactly one of two categories. Recognizing them gives you a checklist for "which kind of rule am I writing?"

**Propagation rules** have *another small-step judgment* (<InlineMath formula="\to" />) above the bar. They say: "if some sub-part can step, the whole construct steps along with it." Examples: the binary-op left/right operand rules, the assignment RHS rule, the print argument rule, the sequencing "step <InlineMath formula="s_1" />" rule, the if-else condition-stepping rule.

**Completion rules** are **axioms** — the only thing above the bar (if anything) is a *bridge*: a side condition (like <InlineMath formula="v_1 \oplus v_2 = v" />), a valuation (<InlineMath formula="\mathcal{N}\llbracket \overline{n} \rrbracket = n" />), an environment lookup (<InlineMath formula="\sigma(x) = v" />), or a value comparison (<InlineMath formula="v = 0" />). They never reference the small-step relation recursively. Examples: the numeral and identifier rules, the operator-application rule, the assignment-completion axiom, the print-completion axiom, the skip rule, both if-else branch-selection rules, and the while unfolding rule.

| Category | Above the bar | What it does | Examples |
|---|---|---|---|
| **Propagation** | A <InlineMath formula="\to" /> judgment | Steps a sub-part; the whole steps with it | binary-op left-stepping, assignment RHS-stepping, sequencing first-statement, if-condition-stepping |
| **Completion** | Bridges only (or empty) | Performs the effect or selects the rule | numeral, identifier, operator-apply, assignment-bind, print-emit, skip, if-truthy, if-falsy, while-unfold |

The pattern is universal for any SOS you write. **Propagation rules wire up the recursion; completion rules do the actual work.** When you derive rules from a tree grammar, ask of each child: does it need to be reduced first? If yes, write a propagation rule for it. Once all children are values, write one completion rule that performs the construct's effect. This is the same compositional discipline as the binary-op three-rule split — generalized to every construct in the language.

---

## Small-Step vs. Big-Step

| Small-Step (SOS) | Big-Step (Natural) |
|---|---|
| Shows each tiny reduction | Shows final result directly |
| One step at a time: $\to$ | One big evaluation: $\Downarrow$ |
| Describes non-terminating programs as infinite sequences | Cannot describe non-terminating programs at all |
| Describes thread interleaving | Cannot express interleaving |
| More rules, more detail | Fewer rules, more compact |
| Gordon Plotkin, 1981 | Gilles Kahn, 1987 |

Small-step has *richer expressive granularity*: it can describe programs that don't terminate (as infinite step sequences) and concurrent programs (as interleavings of steps from different threads). Big-step is more convenient: it's closer to how we think about evaluation, and it maps directly to a recursive evaluator. We'll sharpen what "richer" means in a moment.

> An important practical difference: when a program has a bug, small-step semantics lets you inspect the *exact step* where things went wrong. Big-step can only tell you that the entire evaluation failed. This is why debuggers (like `gdb` or Chrome DevTools) work in a small-step style — they let you "step through" one instruction at a time.

### Equivalence — for Terminating Programs

Before going further, we should pin down a question students always wonder about: when both semantics apply, do they *agree*? The answer is yes, and it's important enough to state as a theorem (we won't prove it, but the proof technique is induction on the proof tree for one direction and induction on the length of the transition sequence for the other).

<DefinitionBox term="Equivalence Theorem (informal)">

For any Petal program <InlineMath formula="P" /> and initial state <InlineMath formula="\langle \sigma, o \rangle" />, **if the program terminates**, then small-step and big-step produce the same final state:

<DisplayMath formula="\langle P, \sigma, o \rangle \to^{*} \langle \sigma', o' \rangle \quad \iff \quad \langle P, \sigma, o \rangle \Downarrow \langle \sigma', o' \rangle" />

where <InlineMath formula="\to^{*}" /> denotes the reflexive-transitive closure of the small-step relation (zero or more steps).

</DefinitionBox>

This is the formal justification for talking about "the meaning" of a Petal program — both semantics, *when they both have something to say*, say the same thing.

#### Wait — if they're equivalent, why did I say small-step is more powerful?

Good question. The equivalence theorem above has a critical qualifier: **if the program terminates**. That's the whole story. The two semantics agree everywhere they both define a result, but they *differ in what they can describe outside that domain*.

Look closely at what each semantics actually *is*:

- **Big-step is a relation between configurations and final values.** A judgment <InlineMath formula="\langle P, \sigma, o \rangle \Downarrow \langle \sigma', o' \rangle" /> exists in the relation iff there's a *finite* proof tree for it. If the program runs forever, no finite tree exists, so the pair is *not in the relation at all*. Big-step's <InlineMath formula="\Downarrow" /> literally contains no entries for non-terminating programs — they simply have no big-step meaning.

- **Small-step is a transition relation between configurations.** Each step <InlineMath formula="\langle e, \sigma \rangle \to \langle e', \sigma' \rangle" /> is itself finite, but you can chain them into a sequence of any length — including an *infinite* sequence. A non-terminating program is described by an infinite sequence of finite steps, which is a perfectly well-defined mathematical object (it's a function <InlineMath formula="\mathbb{N} \to \text{Config}" />).

So the asymmetry is **not** that small-step "computes more"; it's that small-step has a richer vocabulary for talking about *what kind of behavior a program has*. They agree on what terminating programs mean. They disagree on whether non-terminating programs have *any* meaning at all — big-step is silent, small-step describes them as infinite reduction sequences.

The same explains the concurrency point: small-step lets you describe interleavings (sequences of steps from multiple threads in any order), while big-step compresses each thread to a single judgment with no room for interleaving. It's not extra computational power — it's extra *expressive granularity*.

> This asymmetry will reappear in LN 12 with denotational semantics. There, non-terminating programs receive a denotation too — but only because we'll introduce a special "bottom" element <InlineMath formula="\bot" /> to give them a value. The denotational story is "let's give every program *some* meaning, even if it's the meaning of being undefined." Big-step's silence on non-termination is exactly the gap that fixed-point theory will fill. So the question of whether `while true {}` has meaning gets three different answers across the three approaches: *no* (big-step), *an infinite sequence* (small-step), *the value <InlineMath formula="\bot" />* (denotational).

### Stepping Through

The `OperationalSemanticsStepper` below lets you compare both modes on the same Petal program. In big-step mode, entire subtrees collapse to values. In small-step mode, you see each individual reduction. Toggle between them to feel the difference:

<OperationalSemanticsStepper
  title="Evaluating: x = 3 + 5; print x;"
  astNodes={[
    { id: "prog", label: "Program", level: 0 },
    { id: "a1", label: "x = ...", level: 1 },
    { id: "add", label: "+", level: 2 },
    { id: "n3", label: "3", isTerminal: true, level: 3 },
    { id: "n5", label: "5", isTerminal: true, level: 3 },
    { id: "pr", label: "print", level: 1 },
    { id: "idx", label: "x", isTerminal: true, level: 2 },
  ]}
  astEdges={[
    { from: "prog", to: "a1" },
    { from: "prog", to: "pr" },
    { from: "a1", to: "add" },
    { from: "add", to: "n3" },
    { from: "add", to: "n5" },
    { from: "pr", to: "idx" },
  ]}
  bigSteps={[
    { highlightNodes: ["prog"], rule: "\\frac{\\langle s_1, \\sigma, o \\rangle \\Downarrow \\langle \\sigma', o' \\rangle \\quad \\langle s_2, \\sigma', o' \\rangle \\Downarrow \\langle \\sigma'', o'' \\rangle}{\\langle s_1;\\, s_2, \\sigma, o \\rangle \\Downarrow \\langle \\sigma'', o'' \\rangle}", environment: {}, output: [], description: "The program is s₁; s₂. Apply the sequencing rule: evaluate s₁ in the initial state, then evaluate s₂ with the resulting state. The state threads through." },
    { highlightNodes: ["a1", "add", "n3", "n5"], rule: "\\frac{\\langle 3, \\sigma \\rangle \\Downarrow 3 \\quad \\langle 5, \\sigma \\rangle \\Downarrow 5 \\quad 3 + 5 = 8}{\\langle 3 + 5, \\sigma \\rangle \\Downarrow 8}", environment: {}, output: [], description: "First premise of sequencing: evaluate s₁ = 'x = 3 + 5'. The RHS is 3 + 5. Each numeral applies the valuation function (N⟦3⟧ = 3, N⟦5⟧ = 5), then the side condition gives 3 + 5 = 8." },
    { highlightNodes: ["a1"], rule: "\\frac{\\langle 3 + 5, \\sigma \\rangle \\Downarrow 8}{\\langle x = 3 + 5, \\sigma, o \\rangle \\Downarrow \\langle \\sigma[x \\mapsto 8], o \\rangle}", environment: { x: 8 }, output: [], description: "Assignment rule: the expression evaluated to 8, so bind x to 8. First premise of sequencing complete — state is now (σ' = {x: 8}, o' = ε)." },
    { highlightNodes: ["pr", "idx"], rule: "\\frac{\\sigma(x) = 8}{\\langle x, \\sigma \\rangle \\Downarrow 8}", environment: { x: 8 }, output: [], description: "Second premise of sequencing: evaluate s₂ = 'print x' in the new state. Look up x in σ': x = 8." },
    { highlightNodes: ["pr"], rule: "\\frac{\\langle x, \\sigma \\rangle \\Downarrow 8}{\\langle \\text{print } x, \\sigma, o \\rangle \\Downarrow \\langle \\sigma, o \\cdot 8 \\rangle}", environment: { x: 8 }, output: [8], description: "Print rule: append 8 to output. Second premise complete. The sequencing rule now gives us the final state." },
  ]}
  smallSteps={[
    { highlightNodes: ["prog"], rule: "\\frac{\\langle s_1, \\sigma, o \\rangle \\to \\langle s_1', \\sigma', o' \\rangle}{\\langle s_1;\\, s_2, \\sigma, o \\rangle \\to \\langle s_1';\\, s_2, \\sigma', o' \\rangle}", environment: {}, output: [], description: "The program is s₁; s₂. The sequencing 'stepping' rule says: while s₁ is still reducing, each step inside s₁ becomes a step of the whole program. s₂ waits." },
    { highlightNodes: ["n3"], rule: "\\frac{\\mathcal{N}\\llbracket 3 \\rrbracket = 3}{\\langle 3, \\sigma \\rangle \\to \\langle 3, \\sigma \\rangle}", environment: {}, output: [], description: "Inside s₁ = 'x = 3 + 5': the left operand is the numeral 3. Apply the valuation function N⟦3⟧ = 3. The sequencing rule propagates this step to the program level." },
    { highlightNodes: ["n5"], rule: "\\frac{\\mathcal{N}\\llbracket 5 \\rrbracket = 5}{\\langle 5, \\sigma \\rangle \\to \\langle 5, \\sigma \\rangle}", environment: {}, output: [], description: "The right operand is the numeral 5. Apply N⟦5⟧ = 5. Now both operands are values." },
    { highlightNodes: ["add"], rule: "\\frac{3 + 5 = 8}{\\langle 3 + 5, \\sigma \\rangle \\to \\langle 8, \\sigma \\rangle}", environment: {}, output: [], description: "Both operands are values. Apply the operator side condition: 3 + 5 = 8." },
    { highlightNodes: ["a1"], rule: "\\langle x\\text{=}8, \\sigma, o \\rangle \\to \\langle \\sigma[x \\mapsto 8], o \\rangle", environment: { x: 8 }, output: [], description: "The RHS is now a value (8). Assignment completes: bind x ↦ 8." },
    { highlightNodes: ["prog"], rule: "\\frac{\\langle s_1, \\sigma, o \\rangle \\to \\langle \\sigma', o' \\rangle}{\\langle s_1;\\, s_2, \\sigma, o \\rangle \\to \\langle s_2, \\sigma', o' \\rangle}", environment: { x: 8 }, output: [], description: "s₁ has completed — it reached a terminal configuration. The sequencing 'completion' rule fires: discard s₁, move to s₂ = 'print x' with the new state σ' = {x: 8}." },
    { highlightNodes: ["idx"], rule: "\\frac{\\sigma(x) = 8}{\\langle x, \\sigma \\rangle \\to \\langle 8, \\sigma \\rangle}", environment: { x: 8 }, output: [], description: "Now reducing s₂. The argument of print is the identifier x. Look up x in σ → 8." },
    { highlightNodes: ["pr"], rule: "\\langle \\text{print } 8, \\sigma, o \\rangle \\to \\langle \\sigma, o \\cdot 8 \\rangle", environment: { x: 8 }, output: [8], description: "The argument is a value. Print completes: append 8 to output." },
  ]}
/>

### A Second Example: Nested Expressions

Let's see a more complex example with nested operations to feel how small-step unfolds:

<OperationalSemanticsStepper
  title="Evaluating: y = (2 + 3) * 4; print y;"
  astNodes={[
    { id: "prog", label: "Program", level: 0 },
    { id: "a1", label: "y = ...", level: 1 },
    { id: "mul", label: "*", level: 2 },
    { id: "add", label: "+", level: 3 },
    { id: "n2", label: "2", isTerminal: true, level: 4 },
    { id: "n3", label: "3", isTerminal: true, level: 4 },
    { id: "n4", label: "4", isTerminal: true, level: 3 },
    { id: "pr", label: "print", level: 1 },
    { id: "idy", label: "y", isTerminal: true, level: 2 },
  ]}
  astEdges={[
    { from: "prog", to: "a1" },
    { from: "prog", to: "pr" },
    { from: "a1", to: "mul" },
    { from: "mul", to: "add" },
    { from: "mul", to: "n4" },
    { from: "add", to: "n2" },
    { from: "add", to: "n3" },
    { from: "pr", to: "idy" },
  ]}
  bigSteps={[
    { highlightNodes: ["prog"], rule: "\\frac{\\langle s_1, \\sigma, o \\rangle \\Downarrow \\langle \\sigma', o' \\rangle \\quad \\langle s_2, \\sigma', o' \\rangle \\Downarrow \\langle \\sigma'', o'' \\rangle}{\\langle s_1;\\, s_2, \\sigma, o \\rangle \\Downarrow \\langle \\sigma'', o'' \\rangle}", environment: {}, output: [], description: "The program is s₁; s₂. Apply the sequencing rule: evaluate s₁, then s₂ with the resulting state." },
    { highlightNodes: ["a1", "mul", "add", "n2", "n3", "n4"], rule: "\\frac{\\langle 2 + 3, \\sigma \\rangle \\Downarrow 5 \\quad \\langle 4, \\sigma \\rangle \\Downarrow 4 \\quad 5 \\times 4 = 20}{\\langle (2+3)*4, \\sigma \\rangle \\Downarrow 20}", environment: {}, output: [], description: "First premise: evaluate s₁ = 'y = (2+3)*4'. The nested expression uses numeral valuation (N⟦2⟧ = 2, N⟦3⟧ = 3 → 2+3 = 5), then N⟦4⟧ = 4, then 5 × 4 = 20." },
    { highlightNodes: ["a1"], rule: "\\frac{\\langle (2+3)*4, \\sigma \\rangle \\Downarrow 20}{\\langle y = (2+3)*4, \\sigma, o \\rangle \\Downarrow \\langle \\sigma[y \\mapsto 20], o \\rangle}", environment: { y: 20 }, output: [], description: "Assignment: bind y to 20. First premise of sequencing complete — state threads to s₂." },
    { highlightNodes: ["pr", "idy"], rule: "\\frac{\\sigma(y) = 20}{\\langle \\text{print } y, \\sigma, o \\rangle \\Downarrow \\langle \\sigma, o \\cdot 20 \\rangle}", environment: { y: 20 }, output: [20], description: "Second premise: evaluate s₂ = 'print y' in the new state. Look up y = 20, append to output." },
  ]}
  smallSteps={[
    { highlightNodes: ["prog"], rule: "\\frac{\\langle s_1, \\sigma, o \\rangle \\to \\langle s_1', \\sigma', o' \\rangle}{\\langle s_1;\\, s_2, \\sigma, o \\rangle \\to \\langle s_1';\\, s_2, \\sigma', o' \\rangle}", environment: {}, output: [], description: "The program is s₁; s₂. The sequencing 'stepping' rule: while s₁ is still reducing, each step inside s₁ becomes a step of the whole program." },
    { highlightNodes: ["n2"], rule: "\\frac{\\mathcal{N}\\llbracket 2 \\rrbracket = 2}{\\langle 2, \\sigma \\rangle \\to \\langle 2, \\sigma \\rangle}", environment: {}, output: [], description: "Inside s₁ = 'y = (2+3)*4': the left operand of * is (2+3). Inside that, the numeral 2 evaluates via N⟦2⟧ = 2. Sequencing propagates this step up." },
    { highlightNodes: ["n3"], rule: "\\frac{\\mathcal{N}\\llbracket 3 \\rrbracket = 3}{\\langle 3, \\sigma \\rangle \\to \\langle 3, \\sigma \\rangle}", environment: {}, output: [], description: "The numeral 3 evaluates via N⟦3⟧ = 3. Both operands of + are now values." },
    { highlightNodes: ["add"], rule: "\\frac{2 + 3 = 5}{\\langle 2+3, \\sigma \\rangle \\to \\langle 5, \\sigma \\rangle}", environment: {}, output: [], description: "Apply the operator side condition: 2 + 3 = 5. Now the expression is 5 * 4." },
    { highlightNodes: ["n4"], rule: "\\frac{\\mathcal{N}\\llbracket 4 \\rrbracket = 4}{\\langle 4, \\sigma \\rangle \\to \\langle 4, \\sigma \\rangle}", environment: {}, output: [], description: "The right operand of * is the numeral 4. Apply N⟦4⟧ = 4." },
    { highlightNodes: ["mul"], rule: "\\frac{5 \\times 4 = 20}{\\langle 5 \\times 4, \\sigma \\rangle \\to \\langle 20, \\sigma \\rangle}", environment: {}, output: [], description: "Apply multiplication: 5 × 4 = 20." },
    { highlightNodes: ["a1"], rule: "\\langle y\\text{=}20, \\sigma, o \\rangle \\to \\langle \\sigma[y \\mapsto 20], o \\rangle", environment: { y: 20 }, output: [], description: "RHS is a value. Assignment completes: y ↦ 20." },
    { highlightNodes: ["prog"], rule: "\\frac{\\langle s_1, \\sigma, o \\rangle \\to \\langle \\sigma', o' \\rangle}{\\langle s_1;\\, s_2, \\sigma, o \\rangle \\to \\langle s_2, \\sigma', o' \\rangle}", environment: { y: 20 }, output: [], description: "s₁ has completed. The sequencing 'completion' rule fires: discard s₁, move to s₂ = 'print y' with state σ' = {y: 20}." },
    { highlightNodes: ["idy"], rule: "\\frac{\\sigma(y) = 20}{\\langle y, \\sigma \\rangle \\to \\langle 20, \\sigma \\rangle}", environment: { y: 20 }, output: [], description: "Now reducing s₂. Look up y in σ → 20." },
    { highlightNodes: ["pr"], rule: "\\langle \\text{print } 20, \\sigma, o \\rangle \\to \\langle \\sigma, o \\cdot 20 \\rangle", environment: { y: 20 }, output: [20], description: "Print completes: append 20 to output. Small-step took more steps but showed every detail — including the sequencing handoff." },
  ]}
/>

Notice how small-step took 10 steps where big-step took only 4. The extra steps aren't wasted — they include the sequencing handoff, numeral valuations, and individual operator applications that big-step hides inside its recursive judgments. Small-step shows the *mechanics*; big-step shows the *result*. Both describe the same computation.

---

## Extending Petal

Petal so far has only expressions, assignments, and print. To make semantics interesting, we need **control flow**. But before we dive in, notice what adding a new feature to a language actually requires — it activates the **entire pipeline** we've been building across this course:

1. **Define the concrete syntax** (LN 7–8) — what does the new construct look like as source text?
2. **Update the grammar** (LN 7) — add productions to the CFG so the parser can recognize it.
3. **Update the tree grammar** (LN 8) — add new AST node shapes so the semantic phase knows what to handle.
4. **Define the semantic rules** (LN 11–13) — derive inference rules, equations, or axioms from the new tree grammar nodes.

And if the new feature relies on mathematics not yet in our toolkit — imagine a hypothetical `integrate` keyword that computes definite integrals, or a `sample` expression drawing from probability distributions — we would need to go even further back to the foundations (LN 1–4) and extend our mathematical tools before any of the above steps are possible.

For if-else and while, we don't need new math — the existing tools (booleans encoded as integers, recursion from LN 4) suffice. So we start at step 3: updating the tree grammar. The original `Stmt` production from LN 8 had only two forms:

<DisplayMath formula="s : \text{Stmt} = i\text{=}e \mid \textbf{print}\; e" />

We extend it with two new productions:

<DisplayMath formula="s : \text{Stmt} = i\text{=}e \mid \textbf{print}\; e \mid \textbf{if}\; e\; \textbf{then}\; s^+\; \textbf{else}\; s^+ \mid \textbf{while}\; e\; \textbf{do}\; s^+" />

Both new constructs are statements, not expressions. Each has an expression child (the condition) and one or two statement-list children (the branches or body). The `Exp` and `Program` productions remain unchanged — only `Stmt` grows. Now the tree grammar tells us exactly what rules we must write.

### If-Else

From the tree grammar: <InlineMath formula="\textbf{if}\; e\; \textbf{then}\; s^+\; \textbf{else}\; s^+" /> — one expression child (the condition) and two statement-list children (the branches). This structure tells us we need two rules: one for when the condition is truthy, one for when it's falsy.

```
if x > 0 {
  y = x;
} else {
  y = 0;
}
```

The AST node:
```json
{ "type": "IfElse", "condition": Exp, "then": Statement[], "else": Statement[] }
```

<DefinitionBox term="Conditional Semantics">

The semantics of a conditional is defined by **two rules** — one for each branch. The condition expression is evaluated, and its value determines which branch executes:

<DisplayMath formula="\frac{\langle e, \sigma \rangle \Downarrow v \quad v \neq 0 \quad \langle s_1, \sigma, o \rangle \Downarrow \langle \sigma', o' \rangle}{\langle \textbf{if}\; e\; \textbf{then}\; s_1\; \textbf{else}\; s_2,\; \sigma,\; o \rangle \Downarrow \langle \sigma', o' \rangle}" />

<DisplayMath formula="\frac{\langle e, \sigma \rangle \Downarrow v \quad v = 0 \quad \langle s_2, \sigma, o \rangle \Downarrow \langle \sigma', o' \rangle}{\langle \textbf{if}\; e\; \textbf{then}\; s_1\; \textbf{else}\; s_2,\; \sigma,\; o \rangle \Downarrow \langle \sigma', o' \rangle}" />

</DefinitionBox>

We use the C convention: any non-zero value is "truthy," zero is "falsy." This is a pragmatic choice (LN 6). Notice that both rules have the same conclusion shape — only their premises differ. The dispatch between branches happens *above* the bar, not below it: the syntax of the conditional is identical in both cases; the value of <InlineMath formula="v" /> determines which rule fires.

#### Small-Step (SOS) Rules

The big-step rules above collapse the entire conditional into one judgment. The small-step version makes the condition reduction explicit — each sub-expression in `e` steps individually before the branch is chosen. We need three rules:

<DefinitionBox term="Conditional — Small-Step Rules">

**Reduce the condition** — if `e` can still step, the whole if-else steps by stepping `e`:

<DisplayMath formula="\frac{\langle e, \sigma \rangle \to \langle e', \sigma \rangle}{\langle \textbf{if}\; e\; \textbf{then}\; s_1\; \textbf{else}\; s_2,\; \sigma,\; o \rangle \to \langle \textbf{if}\; e'\; \textbf{then}\; s_1\; \textbf{else}\; s_2,\; \sigma,\; o \rangle}" />

**Condition is truthy** — once `e` has reduced to a value $v$ with $v \neq 0$, take the then-branch:

<DisplayMath formula="\frac{v \neq 0}{\langle \textbf{if}\; v\; \textbf{then}\; s_1\; \textbf{else}\; s_2,\; \sigma,\; o \rangle \to \langle s_1,\; \sigma,\; o \rangle}" />

**Condition is falsy** — once `e` has reduced to a value $v$ with $v = 0$, take the else-branch:

<DisplayMath formula="\frac{v = 0}{\langle \textbf{if}\; v\; \textbf{then}\; s_1\; \textbf{else}\; s_2,\; \sigma,\; o \rangle \to \langle s_2,\; \sigma,\; o \rangle}" />

</DefinitionBox>

Compare the two styles: big-step says "evaluate the condition to a value, then run one branch." Small-step says "keep stepping the condition until it's a value, *then* pick a branch." The big-step version hides the intermediate computation inside the premise <InlineMath formula="\langle e, \sigma \rangle \Downarrow v" />; the small-step version exposes every individual reduction step on the condition expression.

> The conditional is where semantics first diverges from pure expression evaluation. Expressions always produce values; conditionals produce *different effects* depending on runtime data. This is the fundamental mechanism that makes programs interesting — without conditionals, every program is a straight-line calculation.

Notice something important: the unused branch ($s_2$ in the truthy case, $s_1$ in the falsy case) is **never evaluated**. This is not just an optimization — it's a semantic decision. The meaning of the conditional depends on only one branch. If the other branch contains an error (like division by zero), it doesn't matter — it's never reached. This connects to LN 10's discussion of short-circuit evaluation: the language deliberately avoids evaluating expressions that aren't needed.

> **Aside: Conditionals across languages.** Different languages package the same underlying semantic pattern (evaluate condition, choose one of two branches, never evaluate the other) in syntactically distinct ways. Python's ternary `y = x if x > 0 else 0` and JavaScript's `const y = x > 0 ? x : 0` are conditional *expressions* — they produce a value. The statement form `if ... else ...` in those languages is a statement — it doesn't. Haskell's `let y = if x > 0 then x else 0` and Rust's `let y = if x > 0 { x } else { 0 }` collapse the distinction: `if` is *always* an expression. Petal follows the C/Java tradition: if-else is a statement, never an expression. The two big-step rules above would need to be rewritten with <InlineMath formula="\Downarrow v" /> conclusions (instead of <InlineMath formula="\Downarrow \langle \sigma', o' \rangle" />) to make Petal's `if` an expression — a small change in the rule shape, a large change in how the language *feels* to use.

### While Loops

From the tree grammar: <InlineMath formula="\textbf{while}\; e\; \textbf{do}\; s^+" /> — one expression child (the condition) and one statement-list child (the body). The self-referential nature of loops means the continuation rule will reference the `while` construct itself in its own premise.

```
while x > 0 {
  x = x - 1;
}
```

The AST node:
```json
{ "type": "While", "condition": Exp, "body": Statement[] }
```

<DefinitionBox term="While Loop Semantics">

The semantics of a `while` loop requires **two rules** — one for termination and one for continuation:

<DisplayMath formula="\frac{\langle e, \sigma \rangle \Downarrow v \quad v = 0}{\langle \textbf{while}\; e\; \textbf{do}\; s,\; \sigma,\; o \rangle \Downarrow \langle \sigma, o \rangle} \quad \text{(loop ends)}" />

<DisplayMath formula="\frac{\langle e, \sigma \rangle \Downarrow v \quad v \neq 0 \quad \langle s, \sigma, o \rangle \Downarrow \langle \sigma', o' \rangle \quad \langle \textbf{while}\; e\; \textbf{do}\; s,\; \sigma',\; o' \rangle \Downarrow \langle \sigma'', o'' \rangle}{\langle \textbf{while}\; e\; \textbf{do}\; s,\; \sigma,\; o \rangle \Downarrow \langle \sigma'', o'' \rangle} \quad \text{(loop continues)}" />

</DefinitionBox>

Notice something remarkable about the "loop continues" rule: the conclusion mentions `while e do s`, and so does the last premise — with **the same syntactic construct**, not a structurally smaller one. This is *unlike* every other rule we've written. The big-step rule for binary operations had premises <InlineMath formula="\langle e_1, \sigma \rangle \Downarrow v_1" /> and <InlineMath formula="\langle e_2, \sigma \rangle \Downarrow v_2" /> — the same judgment form, but applied to *strictly smaller* sub-expressions. The while rule breaks that pattern: the recursive premise references the whole loop again.

This is allowed because we're defining a **relation**, not a **function**. The relation <InlineMath formula="\Downarrow" /> is the *smallest set of pairs* <InlineMath formula="\langle \text{config}, \text{result} \rangle" /> closed under the rules. Self-referential schemata are routine when defining relations this way — think of "the transitive closure of $R$ is the smallest relation containing $R$ and closed under composition." The relation symbol appears on both sides of the definition; nothing breaks because each *instance* of the rule, when the program terminates, unfolds to a finitely-deep proof tree.

> Why mention "function vs. relation" here? Because in LN 12 we'll try to define the meaning of `while` as a **function** — and a function definition that calls itself with the same argument has no obvious base case and no "smallest set" escape hatch. That mismatch is the specific problem fixed points (and the Y combinator from LN 4) will resolve. The self-reference you see today is not the problem; it only becomes a problem when we change the kind of mathematical object we're defining. Operational semantics gets the relational version "for free."

#### Small-Step (SOS) Rules

The standard SOS approach for loops is the **unfolding rule**: on every step, the while construct rewrites itself into a conditional that either executes one iteration and re-enters the loop, or terminates. This requires one new primitive — <InlineMath formula="\textbf{skip}" />, a no-op statement that immediately completes without changing the state.

<DefinitionBox term="While Loop — Small-Step Rules">

**Unfolding** — the loop rewrites into a conditional:

<DisplayMath formula="\frac{}{\langle \textbf{while}\; e\; \textbf{do}\; s,\; \sigma,\; o \rangle \to \langle \textbf{if}\; e\; \textbf{then}\; (s;\; \textbf{while}\; e\; \textbf{do}\; s)\; \textbf{else}\; \textbf{skip},\; \sigma,\; o \rangle}" />

**Skip** — the no-op statement completes immediately:

<DisplayMath formula="\frac{}{\langle \textbf{skip},\; \sigma,\; o \rangle \to \langle \sigma,\; o \rangle}" />

</DefinitionBox>

The unfolding rule is an axiom — no premises needed. It converts the `while` into constructs we already have rules for: if-else and sequencing. After unfolding, the existing if-else SOS rules take over to evaluate the condition and choose a branch. If the condition is truthy, the then-branch executes the body `s` followed by the whole loop again (via sequencing). If the condition is falsy, `skip` fires and the loop terminates.

This reveals something deep: **loops are syntactic sugar for conditional recursion.** The while construct doesn't introduce any new reduction machinery — it simply rewrites into patterns we already know how to handle. This connects directly back to LN 4's fixed points: a loop is a fixed point of the transformation "check the condition, maybe do the body, then repeat." The SOS unfolding makes this structure explicit, while the big-step version hides it inside the self-referential premise.

The `skip` statement is trivial but necessary — it provides the "loop ends" case with a concrete statement to step to when the condition is falsy. Without it, we'd have no way to express "do nothing" as a statement in the SOS framework.

### What About the Concrete Machines?

We've seen how if-else and while work in big-step and small-step abstract semantics. But what do they look like when compiled to our stack machine from earlier in this lecture? Control flow at the machine level is fundamentally **program counter manipulation** — jumps, not rule selection. Two new instructions make this possible: `JUMPZ label` (jump to `label` if the top of the stack is zero, popping the value) and `JUMP label` (unconditional jump).

**If-else compilation:**

```
[compile e]          // push condition value
JUMPZ else_label     // if 0, jump to else branch
[compile s₁]         // then-branch instructions
JUMP end_label       // skip over else branch
else_label:
[compile s₂]         // else-branch instructions
end_label:
```

The condition expression compiles normally, leaving a value on the stack. `JUMPZ` pops it and branches: if zero, execution jumps to the else-branch; otherwise it falls through to the then-branch. The unconditional `JUMP` at the end of the then-branch skips over the else-branch.

**While loop compilation:**

```
loop_label:
[compile e]          // push condition value
JUMPZ end_label      // if 0, exit loop
[compile s]          // body instructions
JUMP loop_label      // go back to condition check
end_label:
```

Loops become **backward jumps**. The `JUMP loop_label` at the bottom sends the program counter back to the condition check, creating the repetition. When the condition evaluates to zero, `JUMPZ` breaks out of the loop by jumping forward past the body.

Notice the contrast with SOS: the small-step approach *unfolds* a while into a conditional (conditional recursion), while the COS approach compiles it into a backward branch (an explicit loop in the instruction stream). These are two very different implementation strategies for the same semantic behavior. The SOS approach stays close to the mathematical definition; the COS approach stays close to how real CPUs work — every `if` in source code becomes a conditional branch instruction, and every `while` becomes a backward branch. The register machine version would look similar, with register operands for the condition check instead of implicit stack values.

---

## From Rules to Code: The Interpreter

Here is the payoff. Every big-step inference rule maps directly to a clause in a recursive JavaScript function. The rules *are* the specification; the code *is* the implementation.

<DefinitionBox term="Tree-Walking Interpreter">

A **tree-walking interpreter** evaluates a program by recursively traversing the AST, visiting each node and computing its meaning directly. Each node type corresponds to a function clause that implements the matching inference rule.

</DefinitionBox>

```javascript
function evaluate(node, env) {
  switch (node.type) {
    case "Number":
      return node.value;

    case "Identifier":
      if (!(node.name in env)) throw new Error(`Undefined: ${node.name}`);
      return env[node.name];

    case "BinaryOp": {
      const left = evaluate(node.left, env);
      const right = evaluate(node.right, env);
      switch (node.op) {
        case "+": return left + right;
        case "-": return left - right;
        case "*": return left * right;
        case "/": return left / right;
        case "**": return left ** right;
        default: throw new Error(`Unknown op: ${node.op}`);
      }
    }

    case "Assignment":
      env[node.name] = evaluate(node.value, env);
      return;

    case "Print": {
      const value = evaluate(node.expression, env);
      console.log(value);
      return;
    }

    case "IfElse": {
      const cond = evaluate(node.condition, env);
      const branch = cond !== 0 ? node.then : node.else;
      for (const stmt of branch) evaluate(stmt, env);
      return;
    }

    case "While":
      while (evaluate(node.condition, env) !== 0) {
        for (const stmt of node.body) evaluate(stmt, env);
      }
      return;

    case "Program":
      for (const stmt of node.body) evaluate(stmt, env);
      return;
  }
}
```

Compare each `case` to the corresponding inference rule:

| Rule | Code |
|------|------|
| $\mathcal{N}\llbracket \overline{n} \rrbracket = n \implies \langle \overline{n}, \sigma \rangle \Downarrow n$ | `case "Number": return node.value` |
| $\sigma(x) = v \implies \langle x, \sigma \rangle \Downarrow v$ | `case "Identifier": return env[node.name]` |
| $\langle e_1, \sigma \rangle \Downarrow v_1, \; \langle e_2, \sigma \rangle \Downarrow v_2$ | `const left = evaluate(node.left, env); const right = ...` |
| $\langle x = e, \sigma \rangle \Downarrow \sigma[x \mapsto v]$ | `env[node.name] = evaluate(node.value, env)` |

The correspondence is one-to-one. Every premise in a rule becomes a recursive call in the code. Every conclusion becomes a return value or state update. The inference rules are not just mathematics — they are **pseudocode for the interpreter**.

> This is not a coincidence. Plotkin designed SOS so that its rules would be directly implementable. The "structural" part of Structural Operational Semantics means the rules are defined by induction on the structure of the syntax — and induction on structure is exactly what a recursive function does.

This is the first implementation of Petal's meaning. Given an AST (the output of the parser from LN 9), this function computes the program's result. We've gone from raw text to meaning:

```
Source → Tokens → CST → AST → evaluate(ast, {}) → Output
                                    ^^^^^^^^^^^^^^^
                                    Today's contribution
```

### The Same Pattern in Other Languages

The tree-walking interpreter pattern is universal. Here's the same `evaluate` function in Python and Haskell:

```python
def evaluate(node, env):
    match node["type"]:
        case "Number":
            return node["value"]
        case "Identifier":
            return env[node["name"]]
        case "BinaryOp":
            left = evaluate(node["left"], env)
            right = evaluate(node["right"], env)
            match node["op"]:
                case "+": return left + right
                case "*": return left * right
        case "Assignment":
            env[node["name"]] = evaluate(node["value"], env)
        case "Print":
            print(evaluate(node["expression"], env))
        case "Program":
            for stmt in node["body"]:
                evaluate(stmt, env)
```

```haskell
evaluate :: AST -> Env -> (Value, Env)
evaluate (Num n) env       = (n, env)
evaluate (Var x) env       = (env ! x, env)
evaluate (BinOp op l r) env =
  let (vl, _) = evaluate l env
      (vr, _) = evaluate r env
  in (applyOp op vl vr, env)
evaluate (Assign x e) env =
  let (v, _) = evaluate e env
  in (v, insert x v env)
```

> Notice how Haskell's pattern matching mirrors the inference rules even more closely than JavaScript's `switch`. Each clause in the Haskell version IS a rule: the pattern on the left is the configuration, the expression on the right is the conclusion. Haskell was designed by programming language theorists — its syntax was crafted to look like mathematical notation.

---

## Interpreters in the Wild

The tree-walking interpreter pattern is not a toy — it's how many real languages work:

```python
# Python: the ast module lets you walk Python's own AST
import ast

tree = ast.parse("x = 3 + 5")
# tree is an AST node, just like our Petal AST
# CPython compiles this to bytecode, but conceptually
# the evaluation follows the same big-step pattern
```

```javascript
// JavaScript: Babel transforms modern JS by walking the AST
const parser = require("@babel/parser");
const ast = parser.parse("const x = 3 + 5;");
// Babel visitors implement semantic actions on each node type
// — the same pattern as our evaluate() switch statement
```

```scheme
; Scheme: the source IS the AST. There is no parse step.
; '(+ 3 5) is both source code and a list — a tree node
; with operator '+ and operands 3, 5. Quoting just stops
; evaluation; eval kicks it back on.

(eval '(+ 3 5) (interaction-environment))   ; => 8

; eval IS the tree-walking interpreter we just wrote.
; It dispatches on the head of each list (the "node type")
; and recursively evaluates the children — the same shape
; as our evaluate() function, but with no parser at all,
; because the syntax tree IS the input.
```

This is the cleanest possible illustration of what we've spent the lecture building toward. **In every other language on this list, you parse text into an AST and then walk it. In Lisp, you skip the parse step — the source code is already a tree.** This property is called **homoiconicity** ("same representation"): a program's source and its data are written the same way. The consequence: the gap between *the inference rules* (what we mean) and *the running code* (what we execute) collapses to almost nothing — `eval` is essentially the rule schema written as a function. This is exactly the equivalence we made visible in the proof-tree split view: program ≡ AST ≡ proof tree ≡ execution. Lisp doesn't have to *demonstrate* the equivalence; it bakes it into the language design.

> Homoiconicity is also what makes Lisp macros so powerful: a macro is just a function that takes an AST (a list) and returns a new AST (another list). No special metaprogramming machinery is needed — you write Lisp that operates on Lisp using the same syntax you use for everything else. We'll see in LN 12 that other languages need elaborate token-level or compile-time facilities (Rust's `macro_rules!`, C's preprocessor, Python's `ast` module) to recover a fraction of what Lisp gets for free.

### Historical Examples

| Language | Interpreter Style | Notes |
|----------|------------------|-------|
| **Lisp / Scheme** | Tree-walking | The original tree-walker (1958). Source IS the AST — no parse step needed. `eval` directly traverses the s-expression. |
| **Early Ruby (MRI)** | Tree-walking | Ruby 1.8 directly walked the AST; Ruby 1.9+ compiles to bytecode |
| **R** | Tree-walking | R's interpreter still walks the AST today |
| **Bash/Shell** | Tree-walking | Shell scripts are parsed and evaluated node by node |
| **Python (CPython)** | Bytecode + VM | Compiles AST to stack machine instructions (the concrete approach) |
| **Java (JVM)** | Bytecode + JIT | Compiles to stack bytecode, then JIT-compiles hot paths to native |

> Robert Nystrom, in *Crafting Interpreters*, calls the tree-walking interpreter "the simplest, most obvious implementation technique." He also notes its limitation: performance. Walking a tree involves pointer chasing (following references from node to node), which is slow on modern CPUs that prefer sequential memory access. That's why production languages typically compile to bytecode — but the semantics are the same.

---

## Proof Trees

Inference rules don't just define evaluation — they also let us **prove** that a specific program produces a specific result.

<DefinitionBox term="Proof Tree (Derivation Tree)">

A **proof tree** (or **derivation tree**) is a tree-shaped composition of inference rules that proves a specific evaluation judgment. Each node in the tree is a judgment, each edge is a rule application, and the leaves are axioms (rules with no premises).

</DefinitionBox>

For the program `x = 3 + 5; print x;`, the proof tree for the first statement looks like:

<DisplayMath formula="\frac{\displaystyle\frac{}{\langle \overline{3}, \sigma \rangle \Downarrow 3} \quad \frac{}{\langle \overline{5}, \sigma \rangle \Downarrow 5} \quad 3 + 5 = 8}{\displaystyle\frac{\langle \overline{3} + \overline{5}, \sigma \rangle \Downarrow 8}{\langle x\text{=}\overline{3} + \overline{5}, \sigma, o \rangle \Downarrow \langle \sigma[x \mapsto 8], o \rangle}}" />

The tree reads bottom-up: to prove that `x = 3 + 5` produces the binding $x \mapsto 8$, we need to prove that `3 + 5` evaluates to `8`, which requires proving that `3` evaluates to `3` and `5` evaluates to `5` (both are axioms — they fire via the numeral bridge with no recursive premises needed).

#### Reading a Proof Tree with the Ω Technique

The Ω technique you learned for individual rules generalizes to whole proof trees. The walk is **post-order**: at each rule application, you trace its own Ω, but instead of jumping straight from premises to conclusion-right, you **recurse into each premise's proof first**. The reader below walks through the proof tree above, applying the Ω at every level and bubbling values back up to the parent rule.

<ProofTreeReader
  title="Reading the proof tree for x = 3 + 5"
  tree={{
    id: "assign",
    ruleName: "Assignment (Big-Step)",
    conclusion: {
      left: "\\langle x\\text{=}\\overline{3}+\\overline{5},\\, \\sigma,\\, o \\rangle",
      right: "\\Downarrow \\langle \\sigma[x \\mapsto 8],\\, o \\rangle"
    },
    premises: [
      {
        id: "add",
        ruleName: "Binary Op (Big-Step)",
        conclusion: {
          left: "\\langle \\overline{3}+\\overline{5},\\, \\sigma \\rangle",
          right: "\\Downarrow 8"
        },
        premises: [
          {
            id: "n3",
            ruleName: "Numeral",
            conclusion: { left: "\\langle \\overline{3},\\, \\sigma \\rangle", right: "\\Downarrow 3" },
            sideConditions: [{ latex: "\\mathcal{N}\\llbracket \\overline{3} \\rrbracket = 3", description: "Numeral bridge: the syntactic numeral 3̄ maps to the value 3. This is an axiom — no recursive premise to discharge." }]
          },
          {
            id: "n5",
            ruleName: "Numeral",
            conclusion: { left: "\\langle \\overline{5},\\, \\sigma \\rangle", right: "\\Downarrow 5" },
            sideConditions: [{ latex: "\\mathcal{N}\\llbracket \\overline{5} \\rrbracket = 5", description: "Numeral bridge for 5̄. Each numeral leaf in the AST gets its own bridge step in the proof tree." }]
          }
        ],
        sideConditions: [{ latex: "3 + 5 = 8", description: "Operator side condition: now that both operands are values (3 and 5), interpret the syntactic + as mathematical addition. This is the syntax-to-semantics bridge for the operator terminal." }]
      }
    ]
  }}
/>

Notice what the walk reveals: the Ω at the root **pauses** at every premise, descends into a sub-proof, completes that Ω fully, then resumes the parent. This is why proof trees feel intimidating — you're tracking multiple recursion levels at once. The reader makes the bookkeeping mechanical: there's only ever *one active rule*, and within it only ever *one part* of the Ω being traversed.

This connects directly to LN 2: inference rules ARE logical deduction. A proof tree for a program evaluation is structurally identical to a proof tree in predicate logic. The rules are the axioms and inference rules of a logical system; the proof tree is a derivation in that system.

### Worked Example: A Larger Proof Tree

Let's build the proof tree for `y = (2 + 3) * 4`:

<DisplayMath formula="\frac{\displaystyle\frac{\displaystyle\frac{}{\langle \overline{2}, \sigma \rangle \Downarrow 2} \quad \frac{}{\langle \overline{3}, \sigma \rangle \Downarrow 3} \quad 2 + 3 = 5}{\langle \overline{2} + \overline{3}, \sigma \rangle \Downarrow 5} \quad \frac{}{\langle \overline{4}, \sigma \rangle \Downarrow 4} \quad 5 \times 4 = 20}{\displaystyle\frac{\langle (\overline{2}+\overline{3}) \times \overline{4}, \sigma \rangle \Downarrow 20}{\langle y\text{=}(\overline{2}+\overline{3}) \times \overline{4}, \sigma, o \rangle \Downarrow \langle \sigma[y \mapsto 20], o \rangle}}" />

The tree structure mirrors the AST structure — each AST node corresponds to one judgment in the proof tree. This is compositionality made visible: the proof of the whole is assembled from proofs of the parts.

> Notice that proof trees and ASTs have a deep structural similarity. Both are trees. Both are defined by induction on their structure. Both decompose a complex entity into simpler parts. This is not a coincidence — it's a consequence of the fact that syntax (ASTs) and semantics (proof trees) are both defined by structural induction, which is itself grounded in the well-foundedness of the natural numbers (LN 3).

### Building a Proof Tree Yourself

Reading proofs is one skill — *constructing* them is another, and arguably more important. When a mathematician writes a proof, they reason **top-down and goal-directed**: start with what you want to prove, look at its syntactic shape, find the rule whose conclusion matches, and the rule's premises become your new sub-goals. Recurse until every leaf is an axiom.

The constructor below mirrors this process. You start with the goal `⟨y = (2+3)*4, σ, o⟩ ⇓ ⟨σ[y↦20], o⟩` as the only "open goal." Each rule in the palette can be clicked — if it matches the conclusion shape of the currently focused goal, the rule expands and its premises become new open goals. Keep applying rules until every leaf is an axiom that has been *deliberately fired*. Even the numeral leaves require you to select the Numeral rule — the bridge `N⟦n̄⟧ = n` doesn't appear above the bar until you choose to apply it. This forces you to acknowledge every step in the proof, no matter how "obvious" it looks.

<ProofTreeConstructor
  title="Build the proof tree for y = (2 + 3) * 4"
  goalDescription="Goal: prove that 'y = (2+3)*4' produces the binding y ↦ 20. Apply the right rule to each open goal — the focused goal is highlighted. Every rule application is your choice, including the axioms (numerals fire via the Numeral rule, which discharges them with the bridge N⟦n̄⟧ = n above the bar)."
  target={{
    id: "assign",
    ruleName: "Assignment",
    conclusion: {
      left: "\\langle y\\text{=}(\\overline{2}+\\overline{3})\\times\\overline{4},\\, \\sigma,\\, o \\rangle",
      right: "\\Downarrow \\langle \\sigma[y \\mapsto 20],\\, o \\rangle"
    },
    premises: [
      {
        id: "mul",
        ruleName: "BinOp",
        conclusion: {
          left: "\\langle (\\overline{2}+\\overline{3})\\times\\overline{4},\\, \\sigma \\rangle",
          right: "\\Downarrow 20"
        },
        premises: [
          {
            id: "add",
            ruleName: "BinOp",
            conclusion: {
              left: "\\langle \\overline{2}+\\overline{3},\\, \\sigma \\rangle",
              right: "\\Downarrow 5"
            },
            premises: [
              {
                id: "n2",
                ruleName: "Numeral",
                conclusion: { left: "\\langle \\overline{2},\\, \\sigma \\rangle", right: "\\Downarrow 2" },
                sideConditions: [{ latex: "\\mathcal{N}\\llbracket \\overline{2} \\rrbracket = 2" }]
              },
              {
                id: "n3",
                ruleName: "Numeral",
                conclusion: { left: "\\langle \\overline{3},\\, \\sigma \\rangle", right: "\\Downarrow 3" },
                sideConditions: [{ latex: "\\mathcal{N}\\llbracket \\overline{3} \\rrbracket = 3" }]
              }
            ],
            sideConditions: [{ latex: "2 + 3 = 5" }]
          },
          {
            id: "n4",
            ruleName: "Numeral",
            conclusion: { left: "\\langle \\overline{4},\\, \\sigma \\rangle", right: "\\Downarrow 4" },
            sideConditions: [{ latex: "\\mathcal{N}\\llbracket \\overline{4} \\rrbracket = 4" }]
          }
        ],
        sideConditions: [{ latex: "5 \\times 4 = 20" }]
      }
    ]
  }}
  rules={[
    {
      ruleName: "Assignment",
      label: "Assignment",
      schema: "\\frac{\\langle e, \\sigma \\rangle \\Downarrow v}{\\langle x\\text{=}e, \\sigma, o \\rangle \\Downarrow \\langle \\sigma[x \\mapsto v], o \\rangle}",
      hint: "Use when the goal has shape ⟨x = e, σ, o⟩ ⇓ ⟨σ', o'⟩."
    },
    {
      ruleName: "BinOp",
      label: "Binary Operation",
      schema: "\\frac{\\langle e_1, \\sigma \\rangle \\Downarrow v_1 \\quad \\langle e_2, \\sigma \\rangle \\Downarrow v_2 \\quad v_1 \\oplus v_2 = v}{\\langle e_1 \\oplus e_2, \\sigma \\rangle \\Downarrow v}",
      hint: "Use when the goal has shape ⟨e₁ ⊕ e₂, σ⟩ ⇓ v."
    },
    {
      ruleName: "Numeral",
      label: "Numeral",
      schema: "\\frac{\\mathcal{N}\\llbracket \\overline{n} \\rrbracket = n}{\\langle \\overline{n}, \\sigma \\rangle \\Downarrow n}",
      hint: "Axiom: applies to any numeral leaf."
    },
    {
      ruleName: "Identifier",
      label: "Identifier",
      schema: "\\frac{\\sigma(x) = v}{\\langle x, \\sigma \\rangle \\Downarrow v}",
      hint: "Use when the goal is ⟨x, σ⟩ ⇓ v for some identifier x. (Distractor here — not needed for this proof.)"
    },
    {
      ruleName: "Print",
      label: "Print",
      schema: "\\frac{\\langle e, \\sigma \\rangle \\Downarrow v}{\\langle \\textbf{print}\\, e, \\sigma, o \\rangle \\Downarrow \\langle \\sigma, o \\cdot v \\rangle}",
      hint: "Use when the goal involves print. (Distractor here.)"
    }
  ]}
/>

Notice the key insight from this exercise: **at every step, the syntactic shape of the goal told you which rule to use.** That's not a coincidence — it's compositionality. The rules are organized by AST node type, and the goal carries that AST shape in its left configuration. Building a proof is just walking down the AST and naming each node's rule.

> The reading walk and the construction walk traverse the same tree in *opposite directions*. Reading is post-order (descend into premises, then resolve the conclusion). Construction is pre-order (apply a rule at the root, then expand each new sub-goal). They produce the same finished proof tree — but reading explains *what holds*, while construction explains *how you knew it would hold*. Mathematicians and interpreter writers both need both skills.

### Proof Trees ARE Programs

Here's the deepest connection in this lecture. The proof tree we just built isn't just a *certificate* that the program evaluates to a particular result — it **is** the program's evaluation. Every rule application corresponds to a piece of source code being evaluated. Every premise corresponds to a sub-piece. Every side condition corresponds to a primitive operation (looking up an identifier, applying a numeral bridge, computing an operator). The proof tree, read leaf-to-root, *reconstructs the program's runtime behavior*.

The split view below makes this correspondence concrete. As you Ω-read the proof tree on the left, the matching source code on the right highlights — and the environment and output panels show the program's state at each judgment. Watch how the source highlight grows as you climb back up the tree, exactly tracking how a real interpreter would expand its frame of focus from sub-expressions to enclosing statements:

<ProofTreeProgramSplit
  title="Proof tree IS the program: x = 3 + 5; print x;"
  programLabel="Petal source"
  program={[
    { id: "tx", text: "x" },
    { id: "teq", text: " = " },
    { id: "t3", text: "3" },
    { id: "tplus", text: " + " },
    { id: "t5", text: "5" },
    { id: "tsemi1", text: ";" },
    { id: "tprint", text: "print", newline: true },
    { id: "tprintsp", text: " " },
    { id: "tx2", text: "x" },
    { id: "tsemi2", text: ";" },
  ]}
  tree={{
    id: "seq",
    ruleName: "Sequencing",
    conclusion: {
      left: "\\langle s_1;\\, s_2,\\, \\sigma,\\, o \\rangle",
      right: "\\Downarrow \\langle \\sigma[x \\mapsto 8],\\, [8] \\rangle"
    },
    programLink: {
      tokenIds: ["tx", "teq", "t3", "tplus", "t5", "tsemi1", "tprint", "tprintsp", "tx2", "tsemi2"],
      environment: { x: 8 },
      output: [8]
    },
    premises: [
      {
        id: "assign",
        ruleName: "Assignment",
        conclusion: {
          left: "\\langle x\\text{=}\\overline{3}+\\overline{5},\\, \\sigma,\\, o \\rangle",
          right: "\\Downarrow \\langle \\sigma[x \\mapsto 8],\\, o \\rangle"
        },
        programLink: {
          tokenIds: ["tx", "teq", "t3", "tplus", "t5", "tsemi1"],
          environment: { x: 8 }
        },
        premises: [
          {
            id: "add",
            ruleName: "BinOp",
            conclusion: {
              left: "\\langle \\overline{3}+\\overline{5},\\, \\sigma \\rangle",
              right: "\\Downarrow 8"
            },
            programLink: {
              tokenIds: ["t3", "tplus", "t5"]
            },
            premises: [
              {
                id: "n3",
                ruleName: "Numeral",
                conclusion: { left: "\\langle \\overline{3},\\, \\sigma \\rangle", right: "\\Downarrow 3" },
                programLink: { tokenIds: ["t3"] },
                sideConditions: [{ latex: "\\mathcal{N}\\llbracket \\overline{3} \\rrbracket = 3", description: "The numeral 3̄ in the source corresponds to the value 3 in the semantic domain. The bridge fires." }]
              },
              {
                id: "n5",
                ruleName: "Numeral",
                conclusion: { left: "\\langle \\overline{5},\\, \\sigma \\rangle", right: "\\Downarrow 5" },
                programLink: { tokenIds: ["t5"] },
                sideConditions: [{ latex: "\\mathcal{N}\\llbracket \\overline{5} \\rrbracket = 5" }]
              }
            ],
            sideConditions: [{ latex: "3 + 5 = 8", description: "The + token in the source corresponds to mathematical addition; with both operands now values, the bridge produces 8." }]
          }
        ]
      },
      {
        id: "print",
        ruleName: "Print",
        conclusion: {
          left: "\\langle \\textbf{print}\\, x,\\, \\sigma',\\, o \\rangle",
          right: "\\Downarrow \\langle \\sigma',\\, o \\cdot 8 \\rangle"
        },
        programLink: {
          tokenIds: ["tprint", "tprintsp", "tx2", "tsemi2"],
          environment: { x: 8 },
          output: [8]
        },
        premises: [
          {
            id: "id",
            ruleName: "Identifier",
            conclusion: { left: "\\langle x,\\, \\sigma' \\rangle", right: "\\Downarrow 8" },
            programLink: { tokenIds: ["tx2"], environment: { x: 8 } },
            sideConditions: [{ latex: "\\sigma'(x) = 8", description: "The identifier x in the print statement looks up its binding in the environment threaded from the previous statement. σ' has x ↦ 8 because the assignment has already fired." }]
          }
        ]
      }
    ]
  }}
/>

Take a moment to appreciate what's happening here. The proof tree on the left and the source program on the right are **two views of the same object**. The proof tree is *what the program means*, expressed as a derivation. The source code is *what the program looks like*, expressed as syntax. The Ω walk that proves the tree is *literally* the same thing as the execution trace of an interpreter running the program. This is the entire point of operational semantics in one picture: **meaning = derivation = execution**.

> This three-way identification — syntax, derivation, execution — is the deepest payoff of the entire lecture. The tree-walking interpreter from earlier traverses the AST. The proof tree traverses the syntax via inference rules. The Ω walk traverses the proof tree. All three are the *same traversal* viewed at three levels of abstraction: the syntax tells you the shape, the rules tell you the semantics, the walk tells you the execution. Reading a proof tree is no different from running a program — it's just running the program in your head, formally.

### Non-Termination and Proof Trees

What about `while 1 { print 0; }`? The proof tree for this program would require:

1. Prove <InlineMath formula="\langle \overline{1}, \sigma \rangle \Downarrow 1" /> via the numeral bridge <InlineMath formula="\mathcal{N}\llbracket \overline{1} \rrbracket = 1" /> (axiom — fine)
2. <InlineMath formula="1 \neq 0" />, so use the "loop continues" rule
3. Execute the body: <InlineMath formula="\langle \textbf{print}\; \overline{0}, \sigma, o \rangle \Downarrow \langle \sigma, o \cdot 0 \rangle" /> (the numeral <InlineMath formula="\overline{0}" /> bridges to the value `0` before being appended to the output stream)
4. Now prove the loop again from step 1, with <InlineMath formula="o' = o \cdot 0" />

The proof tree is **infinitely deep** — it never reaches the base case because the condition never becomes 0. This is big-step semantics' limitation: infinite computation produces an infinitely large proof tree, which is not a valid finite proof. Big-step semantics simply cannot express non-termination.

Small-step semantics handles this naturally — the transition sequence

<DisplayMath formula="\langle \textbf{while}\; \overline{1}\; \textbf{do}\; \textbf{print}\; \overline{0},\; \sigma,\; o \rangle \to \cdots \to \langle \textbf{while}\; \overline{1}\; \textbf{do}\; \textbf{print}\; \overline{0},\; \sigma,\; o \cdot 0 \rangle \to \cdots" />

continues forever. It's an infinite *sequence* of finite steps, which is perfectly well-defined mathematically.

> This asymmetry between big-step and small-step semantics is why language specifications that need to describe non-terminating behavior (like the semantics of concurrent programming languages) almost always use small-step. The JavaScript specification (ECMAScript), for example, uses a small-step style that can describe both terminating and non-terminating programs.

### When Proof Trees Get Stuck

What happens when we try to build a proof tree for an erroneous program?

```
print x;
```

We need to prove $\langle x, \sigma_0 \rangle \Downarrow v$ where $\sigma_0$ is the empty environment. The identifier rule requires $\sigma_0(x) = v$, but $x$ is not in the empty environment. **No rule applies.** The proof tree cannot be completed.

<DefinitionBox term="Stuck Configuration">

A **stuck configuration** is one where the expression is not a value but no transition rule applies. In operational semantics, stuck configurations represent runtime errors — type mismatches, undeclared variables, division by zero. The absence of a rule IS the error handling.

</DefinitionBox>

This is how operational semantics handles errors: the computation "gets stuck." There is no transition that applies, so execution cannot proceed. The program has no meaning under the operational semantics — which is exactly right, because evaluating an undeclared variable *should* be meaningless.

> There is an elegant duality here: well-typed programs never get stuck (this is the **progress theorem** from type theory), and stuck programs are always ill-typed. Type systems, which we'll explore in LN 14, exist precisely to ensure at compile time that no stuck configurations can occur at runtime. This is the bridge from operational semantics to axiomatic semantics — static analysis prevents dynamic errors.

---

## Summary

Today we formalized the first approach to semantics: **operational semantics — meaning by running**. We developed two styles:

**Small-step (SOS)**: Plotkin's transition relation shows each individual reduction. This is the same mechanism as beta reduction in the lambda calculus (LN 4) — you've been doing operational semantics since week four.

**Big-step (Natural)**: Kahn's evaluation relation shows how an expression evaluates to its final value in one judgment. This is more compact and maps directly to a recursive evaluator.

We extended Petal with **if-else** and **while** loops, writing operational rules for each. We noted the self-referential while rule — it works in operational semantics because we're defining a *relation* (closed under the rules), but the same self-reference will require fixed points when LN 12 tries to define meaning as a *function*.

We also distinguished propagation rules (which recursively step a sub-part) from completion rules (axioms whose only "premises" are bridges). And we proved (informally) that small-step and big-step are **equivalent on terminating programs** — they only diverge in how they describe non-terminating ones.

The climax: we showed that the big-step rules map one-to-one to clauses in a JavaScript `evaluate()` function. **The inference rules are the specification; the interpreter is the implementation.** This is the first way to give Petal meaning: run it.

We saw tree-walking interpreters in the wild — from Lisp (where the source code IS the AST, thanks to **homoiconicity**) to early Ruby, R, and shell scripts. The pattern is the same everywhere: recursively walk the AST, applying the semantic rules at each node. Lisp's `eval` is the limit case — when source and tree are the same object, the gap between rules and implementation disappears entirely.

We saw **proof trees** as the formal mechanism for proving specific evaluation results — composing inference rules bottom-up, just like logical derivations in LN 2. We learned to **read** proof trees with the Ω technique generalized across the tree (post-order recursion into premises) and to **construct** them top-down by goal-directed rule selection. The split-view exercise made the deepest connection visible: **a proof tree IS a program's execution** — the same object viewed as syntax, as derivation, and as runtime behavior. We also explored what happens when proof trees fail: **stuck configurations** represent runtime errors, and **infinite proof trees** represent non-termination (something only big-step is silent on; small-step describes them as infinite step sequences).

The key takeaway: **operational semantics is not just theory — it is engineering**. Every interpreter ever written follows this pattern. The gap between the mathematical specification (inference rules) and the running code (the `evaluate` function) is vanishingly small. When you understand the rules, you understand the interpreter, and vice versa.

Next lecture, we'll develop the second approach — **denotational semantics** — which gives meaning not by running programs, but by translating them into mathematical functions. And we'll see that this approach leads to a completely different implementation: a **transpiler**.

<LectureNotes>

**Key Definitions:**
- **Configuration**: a pair $\langle e, \sigma \rangle$ of expression and state
- **Small-step transition**: $\langle e, \sigma \rangle \to \langle e', \sigma' \rangle$ — one step of computation
- **Big-step evaluation**: $\langle e, \sigma \rangle \Downarrow v$ — expression evaluates to final value
- **Inference rule**: premises above the line, conclusion below — the building block of operational semantics
- **Axiom**: an inference rule with no premises — applies unconditionally
- **Proof tree**: a composition of inference rules that proves a specific evaluation result
- **Tree-walking interpreter**: evaluates by recursively traversing the AST
- **Homoiconicity**: a language property where source code is represented as data in the language itself; in Lisp, the source IS the AST, so `eval` traverses program text directly
- **Stack machine**: an abstract device using a LIFO stack for computation
- **Register machine**: an abstract device using named registers for computation
- **Omega (Ω) technique**: a reading strategy for inference rules — trace bottom-left → premises → bottom-right; generalizes to whole proof trees as a post-order recursion
- **Stuck configuration**: a non-value configuration where no transition rule applies — represents a runtime error
- **Conditional semantics**: two rules with the same conclusion shape, dispatched by a side-condition premise on the value
- **While loop semantics**: self-referential rules — work because we're defining a relation, not a function
- **Propagation rule**: an SOS rule whose premise is itself a small-step judgment; "step a sub-part, the whole steps along"
- **Completion rule**: an SOS axiom whose only "premises" are bridges (side conditions, valuations, lookups)
- **Equivalence theorem**: small-step and big-step agree on the meaning of every terminating program; they differ only in what they say about non-termination

**Key Connections:**
- Beta reduction (LN 4) IS small-step operational semantics
- Inference rules (today) ARE logical deduction rules (LN 2)
- Evaluation strategies (LN 4) map to reduction strategies (CBV = reduce args first)
- The tree grammar (LN 8, LN 10) is the API — each production maps to one or more inference rules
- The big-step rules map one-to-one to clauses in a recursive interpreter
- The self-referential while rule works as a relation today; defining `while` as a *function* in LN 12 will require fixed points
- Petal now has control flow (if-else, while), extending the language from LN 8-9
- Proof trees mirror AST structure — both are defined by structural induction (LN 3)
- Reading a proof tree = constructing a proof tree = executing the program — three views of one object
- Stuck computations model runtime errors (connects to LN 10's error discussion)
- Stack vs. register machines illustrate pragmatic design choices (connects to LN 6)
- Numeral/number distinction: every syntactic value in a rule that "looks like a value" is really a metavariable plus a side condition that bridges to the value

</LectureNotes>

<LectureResources>

### Additional Resources

- [Dr. Ray Toal, Operational Semantics Notes](https://cs.lmu.edu/~ray/notes/opsem/) — Reference material with Astro and Bella examples
- [Kenneth Slonneger and Barry Kurtz, *Formal Syntax and Semantics of Programming Languages*, Chapter 8](https://cdn.preterhuman.net/texts/science_and_technology/artificial_intelligence/Formal%20Syntax%20and%20Semantics%20of%20Programming%20Languages%20-%20Kenneth%20Slonneger.pdf) — Operational semantics
- [Gordon Plotkin, "A Structural Approach to Operational Semantics" (1981)](https://homepages.inf.ed.ac.uk/gdp/publications/sos_jlap.pdf) — The original SOS paper
- [Crafting Interpreters — Robert Nystrom](https://craftinginterpreters.com/) — Practical guide to building a tree-walking interpreter
- [Gilles Kahn, "Natural Semantics" (1987)](https://hal.inria.fr/inria-00075953/) — The original Natural Semantics paper
- [Peter Sestoft, "Demonstrating Lambda Calculus Reduction"](https://www.itu.dk/~sestoft/papers/sestoft-lamreduce.pdf) — Lambda calculus evaluation as operational semantics

</LectureResources>
